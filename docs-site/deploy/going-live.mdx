---
title: "Going live"
description: "From a generated build to production: what the compiler gives you and what stays yours."
---

This page is about the **code targets**, pipecat and livekit. `unmute compile`
hands you a project, and deploying it is deploying a normal Python container.
Unmute is not involved at run time and is not a dependency of the thing you ship.

<Note>
A **slng** target is the other shape: SLNG hosts the agent, so there is no
container, no image and no platform step. One command does the whole thing — see
[Deploy to SLNG](/deploy/slng).
</Note>

## What you get, what you provide

| The compiler emits | You provide |
|---|---|
| the Python project and a `Dockerfile` | somewhere to run containers |
| an `.env.example` naming values you supply | the values, from your secret store |
| Compose files for local development | production networking, TLS, DNS, ingress |
| a deploy manifest and a compile report | Redis where the route needs it, scaling, monitoring |
| a `README.md` runbook written for that platform | the decision to press go |

The same image runs locally and in production. Only the start command and the
environment change.

## The runbook is in the build

Every `build/<target>/README.md` is written for that exact build: its required
variables, its platform commands, and its carrier steps if it has a phone
route. It is the authority for that project, because it was generated from the
same package the code was.

Read it before you deploy. This page is the shape; that file has your values.

## Secrets

Generated files and reports carry environment variable **names**, never values.
`.env.example` lists the values you supply for this build. The compile report
has the complete required set, including names supplied by the runtime or
platform:

```text
# Environment for livekit (generated by unmute).
# Copy this file to `.env` and fill in the values. Never commit `.env`.
OPENAI_API_KEY=
SLNG_API_KEY=
```

That is a browser-only LiveKit build, the shape `unmute init` scaffolds. The
worker still requires `LIVEKIT_URL`, `LIVEKIT_API_KEY`, and
`LIVEKIT_API_SECRET`, but the runtime or operator supplies them, so they stay
in `compile-report.json` rather than this to-do list. The list is per build;
read the one your compile wrote.

Both platforms take a secrets file on the first deploy. After that, changing a
value is one command, and it belongs to your secret store, not to the
repository.

<Warning>
  Every variable name must be a valid shell identifier: letters, digits, and
  underscores, never starting with a digit. Platforms export secrets through a
  shell, so a name like `2FACTOR_API_KEY` fails at export and the value is simply
  missing at run time. The compiler refuses one before you get there:

  ```text
  connections/twilio_sip.yaml:6: connection "twilio_sip" environment sip_password is not a
    valid environment variable name: use upper case letters, digits, and underscores, and
    do not start with a digit. This field takes a name and never a value. A deployment
    platform exports secrets through a shell, so a bad name would be missing at runtime
    with no error of its own
  ```
</Warning>

## Sizing

The compile report turns your `capacity:` block into worker and quota numbers:

```text
pipecat: sizing workers=10 [unbenchmarked] (2026-07-15 conservative 1 session per worker/GPU; channels=realtime_audio)
```

`[unbenchmarked]` is not decoration. The numbers come from a conservative
assumption, one session per worker, not from a measurement of your agent. Use
them as a starting point and then measure.

## Production concerns, both targets

- **Redis** is needed for LiveKit SIP and a multi-node self-hosted LiveKit
  Server. Pipecat `cloud-websocket` and `daily-sip` do not use it.
- **Separate environments.** Keep production and staging apart, and never point
  a development worker at a production server.
- **Logs.** Workers and the Pipecat application log to stdout. Ship those logs.
  Pipecat logs default to `INFO`; set `UNMUTE_LOG_LEVEL=DEBUG` only in a
  controlled environment because logs can contain caller speech, tool data,
  and phone details.
- **Model providers.** Hosting the orchestrator yourself does not host the
  models. Speech and reasoning calls still go to the providers you bound.

## Hosting the generated container yourself

The managed-cloud pages are the supported deployment path, but the generated
projects are ordinary containers and can run on infrastructure you control.

For LiveKit, the agent worker connects out to LiveKit Server and needs no
inbound port. The server is the public edge: it needs a trusted TLS domain,
public WebRTC ports, and TURN for callers behind strict firewalls. Add LiveKit
SIP beside it for phone calls, with public SIP signalling and RTP. An HTTPS
tunnel is not enough for SIP.

For Pipecat browser calls, expose the bot port over HTTPS and make WebRTC media
reachable. Cross-network calls need STUN or TURN; the generated SmallWebRTC
configuration does not add ICE servers today, so treat that as required work
before a self-hosted browser deployment.

Pipecat's phone routes both stay light on your infrastructure. The
`cloud-websocket` route hosts nothing of yours: Pipecat Cloud terminates the
carrier's media itself. The `daily-sip` carrier form emits only a small
webhook helper; the deployed agent still exposes no inbound service.

Scale by active sessions, not HTTP request rate. Keep each long-lived
WebSocket on the replica that accepted it, and give shutdown enough time to
drain the longest call. LiveKit distributes jobs to registered workers; do not
put a load balancer in front of those workers.

## Phone routes

<Note>
  A phone call reaches an agent that is deployed. Deploy it, finish the
  carrier setup below, then call the number: that is how you confirm the
  phone path works. `unmute dev` covers the prompt, tools, and models in the
  browser, and stops where the phone leg starts.
</Note>

If the target declares a phone route, the generated README carries the carrier
steps: the markup to paste, the trunk to configure, or the number to attach.
Those steps are per route and per carrier, and they are the part no compiler can
do for you.

Route maturity is printed in the compile report:

```text wrap
livekit: telephony evidence inbound=provisional docs=https://docs.livekit.io/telephony/start/providers/twilio/ verified=2026-08-12 smoke=false
```

See [the compile report](/reference/cli/compile#the-report) for each field.

## Changing the agent

Edit the package, compile again, deploy again. Never edit `build/`.

```sh
unmute validate ./my-agent
unmute compile ./my-agent
```

### Except changing its name

[`name:`](/reference/agent-yaml#name) is the one field where "deploy again" is
not enough, because joined to the target it *is* the deployed thing's identity.
A rename does not move a deployment. It leaves the old one where it was and
addresses a new one.

| Target | What the next deploy does | What you do |
|---|---|---|
| `slng` | pushes a second agent under the new name | delete the old agent, or point your sessions at the new id |
| `pipecat` | creates a second agent; the old one keeps running and keeps billing | deploy, confirm `ready`, then `pipecat cloud agent delete <old-name>`; create the secret set under the new name first |
| `livekit` | updates the same agent in place, under the new worker name | delete the SIP dispatch rule naming the old worker, then re-run `telephony-setup.sh`; see [renaming the agent breaks that rule](/deploy/livekit-cloud#renaming-the-agent-breaks-that-rule) |

On `livekit` the agent survives because `lk agent deploy` targets the id in the
preserved `livekit.toml`. On the other two the name is how the platform finds
the resource, so a new name means a new resource.

## Where to go next

<Columns cols={2}>
  <Card title="LiveKit Cloud" icon="cloud" href="/deploy/livekit-cloud">
    The `lk` CLI path, in order.
  </Card>
  <Card title="Pipecat Cloud" icon="cloud" href="/deploy/pipecat-cloud">
    The `pipecat` CLI path, in order.
  </Card>
  <Card title="CLI reference" icon="terminal" href="/reference/cli/overview">
    Every command, flag, and exit code.
  </Card>
  <Card title="Models" icon="list" href="/models/stt">
    Which vendors each target supports, per role.
  </Card>
</Columns>
