---
title: "Choosing a structure"
description: "Tools, tasks, task groups, or a second agent. Which split to reach for, and when."
---

You have four ways to organize an agent. Each one owns a different boundary.
Choose from the brief before you write files.

## Choose the native shape

| The brief needs | Native shape | Do not invent |
|---|---|---|
| A real external or local action | `tool` | A task used only as an API wrapper |
| One bounded step that returns | `task` | A second agent or progress flags |
| Several steps in a fixed order | `task group` | Current-step variables or transition tools |
| A lasting role or permission change | `agent handoff` | A returning task |
| A runtime value needed across a boundary | `variable` | Conversation memory or workflow state |

If none of these boundaries exists, keep one agent with a clear prompt and its
real tools. You describe the job; the coding agent should choose the shape and
tell you what it chose. You do not need to ask for a task or task group by name.

A server-directed sequence is dynamic, even when its response calls the field
`nextStep`. Put that loop in one task that asks the returned question and calls
the real domain tools. If fixed stages surround the loop, those stages can be
tasks in a task group. The server owns its dynamic order.

## Do not rebuild the orchestrator

- Do not create current-step, happy-path, completion, or routing variables.
  Tasks, groups, handoffs, and an external server already hold that state.
- Do not create advance, proceed, dispatcher, or transition tools. A tool does
  real external or local work; a native control moves the conversation.
- Keep a variable only when its value really crosses a boundary or feeds a later
  tool call.

## The symptom decides the shape

| What you are seeing | The shape that fixes it |
|---|---|
| the prompt keeps growing and starts contradicting itself | split it: [tasks](/build/orchestration/tasks) if the parts serve one caller goal, a [handoff](/build/orchestration/handoffs) if they are separate roles |
| the model does things out of order | a [task group](/build/orchestration/task-groups): the order is declared, not requested |
| the model calls a tool it should not have yet | move the tool. Lists are per agent and per task, so a tool the current step does not hold cannot be called at all |
| a step runs before you have the value it needs | `requires:` on that delegate. The step is held back, and the model is told which control fills the gap |
| you need a value out of a step and want to keep it | a task with `result:`, delegated with `assign:` |
| two phases need different tools or different permissions | a [handoff](/build/orchestration/handoffs) |
| the caller needs a person | none of these: that is a [human transfer](/transfers/overview), and what it can do depends on the phone route |

A prompt that says "always identify the caller first" is a request. A task group is
a guarantee, and so is `requires:` on the step that needs the identity. That is
the difference you are buying.

Reach for the task group when the *order* is what must hold, and for `requires:`
when one *value* must be there before one step runs. The second is cheaper: no
extra step, no extra prompt site, and nothing the caller has to be spoken
through.

## What each shape costs

<Columns cols={2}>
  <Card title="One agent and tools" icon="bot">
    **Control:** stays with the agent for the whole call.

    **Context:** the whole conversation.

    **Correction:** ask again.

    **Cost:** the prompt carries everything, and grows.
  </Card>
  <Card title="Task" icon="list-checks">
    **Control:** returns when the task finishes.

    **Context:** what `context:` gives the task.

    **Correction:** delegate again.

    **Cost:** one more prompt and tool list to keep straight.
  </Card>
  <Card title="Task group" icon="list-ordered">
    **Control:** returns when the group finishes.

    **Context:** shared across the steps in the group.

    **Correction:** revisit a step inside the group.

    **Cost:** an order you have to be sure about.
  </Card>
  <Card title="Handoff" icon="arrow-right-left">
    **Control:** leaves for good.

    **Context:** only what `context:` carries over.

    **Correction:** another handoff back.

    **Cost:** nothing returns, so there is no result or automatic way back. The
    receiving agent speaks on arrival, so every handoff costs the caller a turn.
  </Card>
</Columns>

These are not exclusive. One agent can delegate a task in one phase and hand off in
another.

For a combined release-readiness package, see
[`examples/salon-concierge`](https://github.com/slng-ai/unmute/tree/main/examples/salon-concierge).
It uses a guarded delegate, a typed task, one handoff, and a human transfer in
one inbound-only project. Two agents, because the second one holds a permission
and a document set the first must not.

## Delegate or transfer

The difference is whether control comes back. That is why they are two blocks
rather than one block with a kind field: which block a thing is written in is
what it is.

```yaml
delegates:
  check_customer:          # runs, returns a typed result, the agent continues
    task: customer_record
    when: Identify the caller before handling an appointment request.

handoffs:
  to_appointment_manager:  # hands the call over, nothing returns
    to: appointment_manager
    when: The caller wants to reschedule or cancel an existing appointment.
```

### Guard the step, not the route to it

Both kinds take `requires:`, and on a delegate it is usually the better place for
it. Put the prerequisite on the work that needs the value:

```yaml
delegates:
  manage_booking:
    task: booking
    when: The caller wants to make, change, or cancel a booking.
    requires:
      - customer_phone
```

The step will not start while `customer_phone` is empty. The refusal goes to the
model, not to the caller: it names the missing variable and the delegate that
supplies it, so the model runs that delegate and calls the step again on the
same turn. See [Tasks](/build/orchestration/tasks#guarding-a-step) for what the caller
hears, and what happens when the model cannot recover.

Do not add an agent whose only job is to hold a guard in front of a step. That
agent has to be spoken through, which costs the caller a turn and buys nothing a
`requires:` on the step does not already give. And do not guard the route to a
person: someone who asks for a manager should not be interviewed first.

| | `delegates:` | `handoffs:` |
|---|---|---|
| returns | yes | no |
| typed result | yes, `result:` | no |
| targets | a task or a task group | another agent |
| where context is set | `context:` on the task | `context:` on the handoff entry |
| `requires:` | yes | yes |

## Where to go next

<Columns cols={2}>
  <Card title="Run it locally" icon="play" href="/dev/overview">
    The dev loop in depth: ports, logs, and picking a target.
  </Card>
  <Card title="Telephony" icon="phone" href="/telephony/overview">
    Put the agent on a phone number.
  </Card>
</Columns>
