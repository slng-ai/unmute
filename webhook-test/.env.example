# Starter keys for the selected target. Copy to `.env`, fill in, then run
# `unmute dev webhook-test`. `unmute compile` writes an exact per-target
# build/<target>/.env.example from your spec.
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=
LIVEKIT_URL=
OPENAI_API_KEY=
SLNG_API_KEY=
