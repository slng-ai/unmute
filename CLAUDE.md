# Unmute CLI

Go CLI that compiles a declarative voice-agent spec into orchestrator-native artifacts. `docs/ARCHITECTURE.md` explains the design and points at the load-bearing code. Go structs and `internal/target` own machine behavior; `docs-site/` is the public user guide. Local feature work lives in ignored `specs/<nnn>-<slug>/` directories.

## Voice contracts
While writing documents or speaking with the user, always use a simple language and simple wording. 

## The one rule
Unmute is written in Go, so you maintain **Go code**, but you also write some Python code snippets and examples, in Python. Checked-in Python has to pass `ruff check .` (CI enforces it). Run `ty check` too when you have the provider SDKs installed, otherwise it only reports imports it cannot resolve.

## Tooling
- Go 1.26 (pin in `go.mod`, and keep it on a Go line that still gets security patches, which is the newest two); `CGO_ENABLED=0` static binary; version stamped at link time, never hardcoded.
- Direct deps — `cobra`, `goccy/go-yaml` (gives line/col on parse errors), `google/jsonschema-go` (**v0.x — pin the exact version, bump deliberately**), and the Charm TUI stack: `charmbracelet/bubbletea` + `bubbles` + `lipgloss` power the interactive console (custom MVU styled with Lip Gloss), while `charmbracelet/huh` v1.0.0 is scoped to the accessible/headless renderer only. **The interactive path imports no `huh`; Lip Gloss is expected there. All color lives in `internal/style` — no color literal anywhere else** (guarded by `internal/style/literal_test.go`, which walks every Go string literal in the tree). Everything else is stdlib. **No new dep for what a few lines of stdlib do — justify any addition in the PR.** No `viper` until a real global config file exists.
- `golangci-lint` from day one (`.golangci.yml`).
- Make targets: `build test smoke contracts lint fmt install`. `contracts` re-fetches the published SLNG conformance fixtures and fails on a digest mismatch: network, no accounts.

## Command rules (cobra) — these are what make commands testable, not suggestions
1. Build the tree with a `newRootCmd()` constructor; **no package-level `var rootCmd`** (fresh tree per call = flag isolation between tests).
2. Write to `cmd.OutOrStdout()` / `cmd.ErrOrStderr()`, **never `fmt.Println`** (a stray Println is invisible to tests — it's a bug).
3. `RunE`, never `Run`. **No `os.Exit` / `log.Fatal` inside a command** — return errors wrapped with `%w`.
4. `os.Exit` lives only in `main.go`. `Execute()` builds the root, prints any error once to stderr, returns the exit code.
- `SilenceUsage` + `SilenceErrors` on the root. Exit codes: `0` ok, `1` error — add more only when a consumer actually reads them. Warnings → stderr + exit 0 (never a silent downgrade).

### A command says what it did, and what somebody has to fix
Nothing else. `unmute --version` is one line, `validate` is its result rows,
`compile` is the list of files it wrote, and `init` is the list of files it
created. A warning, a prerequisite or an error is added to that; a description
of the output is not, because the output is on disk and
`build/<target>/compile-report.json` is next to it.

This is not taste, it is what makes the warnings readable. Every forwarded
binding, derived worker count and resolved route used to print on every run
alongside eight advisory notes about how a framework works, so twenty-odd lines
of nothing-to-do buried the two lines that said a secret was undeclared and a
tool call would 400. **A new line on stdout has to name something the reader
did or has to do.** A new `Warn` row in the capability table has to name a
difference the author can act on, and the table is allowed exactly one
(`internal/target/table_test.go`), so adding a second is a deliberate decision
and not a place to park a note to self.

## IR
Go structs are the schema source for their own surface: `internal/spec` derives the unresolved authoring schema, while `internal/ir` derives the resolved/debug schema. **Do not hand-author `.json` schema files.** Flow: `spec.Load` → `ir.Build` → `ir.Validate` → `generate.Generate`.

### Typed session state
A variable's `type:` is a single-line Python type expression, and `shapes:` is
a top-level **list** of named field groups it can refer to. A task's `assign:`
derives its finish fields from the destination variables, so authors declare
each type once. The grammar and error column live in
`internal/spec/typeexpr.go`; the vocabulary and advice live in
`internal/ir/shapes.go`. The shared emitted Python lives in
`internal/generate/shapes.go` and is inserted into both code targets verbatim.

No saved value enters a prompt automatically. A prompt grants access by naming
`{{variable}}` or `{{variable.field}}`. Omitted task and handoff history means
spoken `messages`; `reset` receives no old conversation. Task return restores
the owner's earlier context and adds only a completed or unserved status.

### No dictionaries in the authoring surface
**A new authored field is a list, never a map.** An entry with a name carries the name as a field (`- name: caller`); a mapping from one name to one value is a list whose every item holds exactly one key (`- customer_phone: result.value`), decoded by an `UnmarshalYAML` into a struct so no `map[...]` reaches the Go type. Two reasons, and the second is the one that bites: a map has no order a reader can see, so a file that lists three things says nothing about which runs first; and a map field cannot carry a per-entry comment where anybody will find it.

This is a **ratchet**, not a migration. Every map field that exists today is on the allowlist in `internal/spec/no_dictionaries_test.go`, in two sections that mean different things:
- **Permanent.** `input:`, `output:` and `params:` are JSON Schema or provider passthrough. A JSON Schema object *is* a dictionary; converting it would stop it being one. These never move.
- **Debt.** The rest carry a one-line reason and a "migrate when". This section may shrink. It must never grow.

Adding a map-typed authored field fails `TestNoNewDictionaryInTheAuthoringSurface`, which names the field and says to write a list. `internal/ir` is out of scope: it is the resolved shape, not something a person writes.

## Testing
`make test` (`go test -race ./...`) runs L1–L3 and needs **zero Python**:
- L1 unit (pure logic, table-driven) · L2 in-process command tests (real tree, capture output) · L3 golden files (`-update` to regenerate).
- L4 smoke (`make smoke`, build tag `smoke`) proves emitted Python is valid — opt-in, needs Python, never in the default suite or PR gate.
- Telephony is verified on a **deployed** agent, against a real carrier. There is no local phone loop, and no test level stands in for one: `unmute dev` is the browser loop and it stops where the phone leg starts.
- [`docs/HARNESS_TEST.md`](docs/HARNESS_TEST.md) is the reusable prompt for real end-to-end conversations across the examples.
- [`docs/SELF_VERIFY.md`](docs/SELF_VERIFY.md) is how to check runtime behaviour **without** a person on the phone, and it is what to do before asking for a live call. Its first rule: find the layer the defect lives in and reproduce it there. A provider defect is usually one HTTP request, so it needs no audio, no tunnel and no simulated caller — [`scripts/replay_router_scopes.py`](scripts/replay_router_scopes.py) is the worked example. A prompt or seam defect lives in the model's turns, and [`scripts/text_run_livekit.py`](scripts/text_run_livekit.py) drives the emitted LiveKit agent through a scripted text conversation with the real model and real local tools, no audio, so read three of those before asking for a call. Coval evaluates conversations you push to it, so verification never waits on inbound reachability.
- After somebody runs `unmute dev` and talks to the agent, read the call back yourself with [`scripts/read_langfuse_trace.py`](scripts/read_langfuse_trace.py): transcript, tool calls and per-span latency, newest trace by default. Needs the package on `tracing.provider: langfuse`, which `examples/salon-concierge` is. Never describe a call from what you were told about it when the spans are one command away. A call is one trace and one session, with a `turn` span per exchange inside it. Add `--check-v4` after any change to tracing: it fails the run when the call splits into several traces, when its root carries no conversation, when a turn recorded the caller and not the reply, or when an observation is missing the session ID or trace name, none of which is visible in the Langfuse UI.

## Commits and pull requests (advisory)
Plain words, short. Write it the way you would say it out loud, not the way a
release note reads. A pull request body is two parts and nothing else:

- **What's in** — the shape an author writes, in a fence, and one paragraph on
  what it does. What was wrong before, if it takes a line.
- **How it works** — five to ten lines. The decisions somebody would otherwise
  ask about.

The reasoning that filled a body belongs where a reader finds it later: in the
code comment beside the thing it explains, in the gate table above, or in the
commit message. A body that lists every gate is a body nobody reads.

**No tool attribution, no co-author trailer and no session link**, in a commit
message or a pull request. Who wrote it is the author field's job.

## A rule with no gate is a wish
Standards here are not taste, they are things CI or a test can fail on. Writing a new rule into this file means wiring its check in the same PR, or tagging it `(advisory)` so it reads as guidance instead of law.

| Rule | What fails |
|---|---|
| gofmt clean, `go vet` clean, `go.mod` tidy | CI `format` |
| `errcheck` `govet` `staticcheck` `ineffassign` `unused` `errorlint` `forbidigo` | `make lint` |
| no `fmt.Print*` in a command, no `os.Exit`/`log.Fatal` outside `main.go` | `forbidigo`, patterns and reasons in `.golangci.yml` |
| errors wrapped with `%w` and matched with `errors.Is`/`As`, never `==` | `errorlint` |
| L1–L3 green, zero Python, no data races | CI `test`, `go test -race ./...` |
| no color literal outside `internal/style`: no SGR escape, no hex | `internal/style/literal_test.go` (`TestNoColorLiteralOutsideThisPackage`), which reads string literals through the AST so a comment quoting colored output is not a finding, and requires SGR's trailing `m` so cursor control is not one either |
| every command opens with the run header, and none of the branding reaches a pipe: the header is written only to a TTY, and every color goes through `style.For(w)`, which drops to plain text on a non-terminal writer | `internal/cli/ui_test.go` (`TestBrandingLeavesPipedOutputByteIdentical`, `TestV48NoColorOutputIsPlain`) |
| no docs-site page, skill reference or example README quotes CLI output the CLI no longer prints | `internal/docsite/retired_output_test.go` (`TestNoPageQuotesRetiredCLIOutput`), which walks three of the four surfaces |
| the capability table carries exactly one `Warn` row, and it names a difference an author can act on | `internal/target/table_test.go` (`TestDefaultTableIsCompleteAndTyped`) |
| a control the model calls gets a dev-page row of its own, typed `handoff`, and no duration: it hands over to a task rather than returning a result, so it is the row that accounts for the model call after it. Hiding it made a reply that ran one delegate read as two duplicate model calls, and timing it read as a tool that ran for the whole flow | `internal/generate/dev_metrics_test.go` (`TestAControlIsShownWithoutADuration`), `internal/devmetrics/record_test.go` (`TestHandoffIsAnOperationTypeOfItsOwn`), proven at L4 on both targets by `TestSmokeLiveKitDevStreaming` and `TestSmokePipecatDevStreaming` |
| `compile` stdout is the generated file list and nothing else, and every binding, param and sizing figure it used to print is in `compile-report.json` | `internal/cli/compile_test.go` (`TestCompileWritesBindingsAndSizingToTheReport`) |
| no new map-typed field in the authoring surface; a new field is a list, and every existing map is allowlisted as permanent (JSON Schema passthrough) or as debt that may shrink and never grow | `internal/spec/no_dictionaries_test.go` (`TestNoNewDictionaryInTheAuthoringSurface`) |
| a pair-list item holding two keys or none is refused at decode, with its line, because that is what a dropped indent produces and what a `map[string]string` swallowed | `internal/spec/pair_test.go` |
| a package declaring no `prefetch:`, no `confirm:` and no delegate `announce:` emits byte-identical output; the emitted block, its constants and the unconfirmed set appear only when authored | `internal/generate/prefetch_test.go` (`TestPrefetchEmitsNothingForAPackageThatDeclaresNone`) |
| a pre-fetch runs at the seam on both targets, in **authored order**, and never past its budget or into a raise | `internal/generate/prefetch_test.go`, proven at L4 by `TestSmokePrefetchOutcomes` driving resolved, skipped, timed-out and raised through one module |
| `history: messages` drops a tool call together with its reply, because Pipecat's context holds the call as a field on an assistant message and only the reply as its own entry, so a role filter alone leaves a request the provider refuses | `internal/generate/pipecat_v1_test.go` (`TestPipecatMessagesLeavesNoOrphanedToolCall`), which runs the emitted helper over the context a live call 400d on |
| every LiveKit tool span is named after the tool that ran, because the framework names all of them `function_tool` and carries the real name in an attribute, and the private write that renames it is guarded so an upstream rename is a generic name rather than a crash mid-call | `internal/generate/livekit_v1_test.go` (`TestLiveKitNamesAToolSpanAfterItsTool`) |
| a shared emitted log line carries no `%s` and no `.format()`: LiveKit logs through stdlib `logging` and Pipecat through loguru, so either style prints literally on the other target | `internal/generate/prefetch_test.go` (`TestPrefetchLogsCarryNoLibrarySpecificPlaceholder`) |
| an entry reading a value only a later entry assigns is refused, naming both entries and which to move | `internal/ir/prefetch_test.go` (`TestBuildPrefetchRefusesABackwardsOrder`) |
| a value carrying `confirm:` renders in no prompt but its confirming step's, and a tool that silently reads it is refused until the caller has agreed; the mark is cleared through `getattr` so a path that skipped the pre-fetch cannot raise | `internal/ir/prefetch_test.go`, `internal/generate/prefetch_test.go` |
| a `prefetch:` entry running a tool must declare `writes:`, and the refusal says a pre-fetch runs unasked on every call including wrong numbers and hang-ups; `writes:` on a `clock:` or `source:` entry is refused because those run no tool | `internal/ir/prefetch_test.go` (`TestBuildPrefetchRefusesTheSource`, `TestBuildPrefetchTakesEitherAnswerToWrites`) |
| a `writes: true` entry is named in `compile-report.json` and the emitted runbook, and prints **no warning**: the key is required, so a warning would fire forever on every package that legitimately writes | `internal/cli/compile_test.go` (`TestCompileNamesAWritingPrefetchEntryInTheReportAndNotOnStdout`) |
| a tool file still carrying the retired `read_only:` is refused with its line and a sentence naming where the answer moved | `internal/spec/load_test.go`, through `movedToolKeys` |
| a clock entry with no `timezone:` is refused and the message says a container clock is UTC; the zone lives on the entry, a package-level `timezone:` is refused naming where it moved, and two entries may read two zones | `internal/ir/prefetch_test.go` (`TestBuildPrefetchRefusesTheSource`, `TestBuildPrefetchReadsTwoZones`), and the zone resolving in the emitted image is `TestSmokePrefetchZoneResolves` |
| a clock entry reads `now` once into one local and takes every field it assigns off that reading, so an entry assigning date and time cannot straddle a second; every field `ir.PrefetchClockFields` declares has an emitted expression, and `day_of_week` reads a spelled-out tuple rather than `strftime("%A")`, which follows the container's locale | `internal/generate/prefetch_test.go` (`TestPrefetchClockReadsOncePerEntry`, `TestPrefetchEmitsEveryClockField`) |
| an entry's log line names **every** variable it assigned, and every value it writes goes through the length bound, the clock included | `internal/generate/prefetch_test.go` (`TestPrefetchLogsEveryOutcomeByName`, `TestPrefetchBoundsARouterValueAndSaysSo`) |
| one entry assigning one variable twice is refused, naming the variable and the entry once rather than twice | `internal/ir/prefetch_test.go` (`TestBuildPrefetchRefusesTheShape`) |
| a shipped example fills two variables from one lookup, because multi-assign compiled all along and nothing showed it | `internal/generate/prefetch_test.go` (`TestPrefetchFillsTwoVariablesFromOneCall`) |
| a pre-fetch fills a variable that holds one plain value, so shaped text and a `Literal` whose set the result declares are assignable and a `list[...]` or a shape is refused naming the step that produces it; held to `assignableInto`, the same predicate a step's `assign:` uses, because a second one would drift | `internal/ir/prefetch_test.go` (`TestBuildPrefetchRefusesAValueWithFields`, `TestBuildPrefetchFillsShapedText`) |
| `--source` seeds a call fact and reaches the container; `--var` keeps its exact refusal | `internal/cli/dev_vars_test.go`, `internal/generate/prefetch_test.go` (`TestPrefetchSeedReachesTheContainer`) |
| a route grant is a promise to **two** readers: `variables: source:` and `prefetch: source:` resolve through one table, so both Pipecat carrier routes lift the call's facts into the context and `build_state` hydrates a system-source variable from it | `internal/generate/pipecat_cloud_websocket_test.go` (`TestCloudWebsocketLiftsTheCallsFactsIntoTheContext`), `internal/generate/prefetch_test.go` (`TestDailyCarrierLiftsTheCallsFactsIntoTheContext`, `TestPipecatHydratesASystemSourceVariable`) |
| `TelephonyEvidence.Directions` limits and never grants: empty means both, only the two Pipecat Twilio phone numbers set one, and `ResolveTelephonyFeature` does not read it so an absent feature stays flatly `Gated` | `internal/target/telephony_test.go` (`TestTelephonyDirectionsLimitAndNeverGrant`) |
| a fact a route supplies in one direction only warns when the package declares the other, and stays silent when it declares that one | `internal/ir/prefetch_test.go` (`TestValidatePrefetchWarnsWhenTheFactIsSuppliedTheOtherWay`) |
| a refusal or warning naming where a call source *does* work reads the route table rather than a hand-written list, because the hand-written one said the LiveKit routes and would have gone on saying so | `internal/ir/prefetch_test.go` (`TestValidatePrefetchWarnsOnARouteThatSuppliesNoCallFact`), `internal/ir/validate_test.go` (`TestValidatePipecatDailyCarrierCallSources`, `TestValidatePipecatCloudWebsocketCallSources`) |
| a withheld caller ID skips the entry in all three shapes it arrives in: empty, the word `anonymous`, and the keypad digits of a word an upstream carrier sent, which are shaped like a real number | `internal/generate/prefetch_test.go` (`TestPrefetchTreatsAWithheldNumberAsAbsent`) |
| a dictated `<Parameter>` has a real reader, not a mention: the gate looks for `_markup.get("<name>")`, because a substring search passed on the strength of the pre-fetch's own emitted comment | `internal/generate/pipecat_cloud_websocket_test.go` (`TestCloudWebsocketMarkupCarriesNoUnreadParameter`) |
| the docs-site "Where it works" table matches the route table both ways, per route and per direction, and every page teaching the caller's number says it is best effort and that a withheld one arrives as a word | `internal/docsite/prefetch_table_test.go`, `internal/docsite/call_source_scope_test.go` |
| `--var` accepts exactly what the dispatch payload fills, which is `source: call_start` **and** a variable declaring no `source:`, because both drivers hydrate that pair and both runbooks print a `--var` line for each; a runtime-owned source is still refused, and no refusal can render an empty source | `internal/cli/dev_vars_test.go` (`TestCallStartPayload`) |
| every package under `internal/voice-agents-tests/` validates clean on every target it declares and generates, and the directory is never empty | `internal/generate/examples_test.go` (`TestVoiceAgentTestPackagesValidateAndGenerate`) |
| `internal/voice-agents-tests/salon-concierge-v2` keeps the four context choices it exists to show, declares `customer_status` with no default, and shares neither `name:` nor `agent_id` with `salon-concierge` | `internal/generate/examples_test.go` (`TestSalonConciergeV2ScopesEveryStep`) |
| every prompt naming a pre-fetched value reads as a whole sentence when that value is empty | `internal/generate/prefetch_test.go` (`TestPrefetchedPromptsReadWholeWithEveryValueEmpty`) |
| a delegate `announce:` is spoken when the delegate starts, and a delegate with none emits nothing | `internal/generate/delegate_announce_test.go` |
| a tool that silently reads a value through `inject:` or a webhook path is refused while that value is empty or unconfirmed, and the refusal names the step to run first when a task assigns the value or a `confirm:` step confirms it, and says to ask the caller only when nothing supplies it; both targets emit the same text, and a package with no `inject:` emits byte-identical output. This is the one net left for a step run out of order, because `requires:` is gone: ordering is the prompt's job (spec 006) | `internal/generate/templates_lower_test.go` (`TestNeededHintNamesTheRightStep`, `TestRefusalNamesTheSupplyingStepAndAsksForTheRest`), `internal/generate/prefetch_test.go` (`TestPrefetchedValueMeetsTheGateOnlyOnceConfirmed`) |
| every direct dependency is on the allowlist | `internal/cli/deps_test.go` |
| no declaration is reachable only from its own definition | `internal/cli/reachability_test.go` (`deadcode -test ./...`) |
| every declared capability `Field` constant has a row in the table | `internal/target/table_test.go` (`TestEveryFieldConstantHasARow`) |
| every shipped package and the scaffold template is block style, ignoring `{{placeholders}}` and the template's own `[[ ]]` | `internal/generate/examples_test.go` (`TestAuthoredPackagesAreBlockStyle`) |
| nothing authored still speaks the retired `controls:` / `kind:` shape, while a bare `kind:` stays legal on tools, channels and models | `internal/generate/examples_test.go` (`TestNothingAuthoredSpeaksTheRetiredShape`) |
| no page, bundle reference, emitted runbook or package comment still teaches `agent_transfer` or `human_transfer` as a word an author writes; the changelog and generated-code comments are excluded with their reasons inline | `internal/skill/agreement_test.go` (`TestNoReaderFacingSurfaceTeachesARetiredKindName`) |
| every documented YAML example attaches a name under the key its own catalog declares, so a reader copying a page gets something that compiles; checked per file, because one page splits the agents block and the catalog across two fences | `internal/skill/agreement_test.go` (`TestDocumentedExamplesAttachUnderTheRightKey`) |
| a name listed under the wrong kind is refused, and the message names both what the name is and the list it belongs on | `internal/ir/build_test.go` (`TestBuildRefusesANameInTheWrongList`) |
| a task's `handoffs:` survives a console round-trip, because a key `scaffold.Data` does not carry is a key `unmute maintain` deletes at exit 0 | `internal/tui/agent_name_roundtrip_test.go` (`TestMaintainKeepsATasksHandoffs`) |
| every symbol a template names still exists in Go | `internal/scaffold/template_symbols_test.go` |
| every capability row carries a deliberate value for a target `field()` does not seed, and every refusal names what to do instead | `internal/target/table_test.go` |
| the console offers every shipped target, names each correctly, and offers no option validation refuses | `internal/tui/default_target_test.go` |
| the slng target opens no socket to the SLNG agents API | `internal/ir/validate_slng_test.go` |
| an execution kind with no lowering cannot render an HTTP request, and that is two holes rather than one: a kind with no **builder** arm is refused (`TestUnhandledExecutionKindIsRefusedNotPosted`, which calls each builder directly as well as through `Generate`), and a kind with a builder arm and no **template** arm is caught by asserting the emitted method carries no `httpx` (`TestHostedToolLowersOnBothCodeTargets`). Both matter because each code template's tool dispatch ends in `webhook_tool`, so a missing arm renders a POST to an unregistered environment name rather than nothing. Verified by removing each arm and watching the gate fail | `internal/generate/hosted_tool_test.go` |
| a committed mirror is checked against the pin its tool file records, offline, for every selected **code** target, because that target builds and runs the tool itself; a slng target reads no mirror at all, and the algorithm is pinned so a change of hash cannot silently invalidate every pin already committed | `internal/ir/hosted.go` (`validateHostedMirror`, called from `validateTarget` only when `targetcap.EmitsProject`), `internal/ir/hosted_test.go` (`TestMirrorPinIsCheckedOffline`, now run on `slng_hosted_code`; `TestHostedDigestAlgorithmIsPinned`), its inverse `TestSlngNeedsNoMirror`, and `TestSelectingTargetsInEitherOrderGivesTheSameResult` proving the two targets' rows cannot influence each other; `internal/cli/compile_test.go` (`TestCompileNeedsNoCredential`) proves the whole compile needs no credential and no `voiceai` on PATH |
| the scalar and legacy `slng:` forms decode to separate name and pin fields, so a package tool can be traced back to which one it was written with, and a `slng:` reference emits the hosted name into the compiled body while keeping the local tool file's own name for every diagnostic, so the two may differ without renaming or creating anything | `internal/spec/load_test.go` (`TestLoadSlngReference`), `internal/generate/slng_v1_test.go` (`TestSlngEmitsTheHostedNameAndKeepsTheLocalOne`) |
| a hosted tool's declared secrets reach both lists: the package's `secrets:` and the slng vault requirements the runbook prints and the deploy preflight checks, because the slng driver reads the first nowhere | `internal/generate/slng_requirements_test.go` (`TestSlngHostedRequirementsReachTheRunbookAndThePreflight`), and `internal/cli/pull_test.go` (`TestPullNeverWritesASecretValue`) holds the names-only half |
| the slng target creates no tool: `local:` and `webhook:` are refused there, naming the dashboard and `slng:`, and both still work byte for byte on livekit and pipecat | `internal/ir/validate_slng_test.go` (`TestSlngRefusesAuthoredToolBodies`, `TestCodeTargetsKeepAuthoredToolBodies`), `internal/generate/slng_v1_test.go` (`TestSlngV1WritesTheBodyAndTheRunbookAndNothingElse`), and `internal/cli/preflight_test.go` (`TestPreflightReportsAHostedToolTheOrganisationLacks`) |
| every surface naming a `voiceai` command names the same one, agent id included | `internal/target/slng_target_test.go` |
| every deployed identity on all three targets is the package's `name:` joined to its target, never the target alone, while the emitted project's own labels stay the bare name | `internal/generate/deploy_name_test.go` |
| a package with no `name:`, or one that cannot be deployed as written, is refused with the shape and an example | `internal/ir/package_name_test.go` |
| the console preserves an authored name and never renames a package to its folder | `internal/tui/agent_name_roundtrip_test.go` |
| the browser token `unmute dev` mints dispatches to the name the emitted worker registers | `internal/cli/dev_web_test.go` (`TestDevDispatchNameMatchesTheEmittedWorker`) |
| example READMEs name every transport; every `examples/` link resolves | `internal/generate/examples_test.go` |
| every emitted task prompt names its finish call and an escape, every finish takes the reserved `unserved_request`, and the owner is told to read it | `internal/generate/task_prompt_test.go`, `internal/ir/validate_test.go` |
| the skill's tool kinds, vendors, providers and doc pointers match the code | `internal/skill/agreement_test.go` |
| every command and flag the skill names exists | `internal/cli/skill_bundle_test.go` |
| the docs-site CLI pages quote each command's `Usage:` line, not just its flags | `internal/cli/help_capture_test.go` |
| a receiving agent's opening turn withholds its own handoffs, and no emitted tool carries the on-enter flag | `internal/generate/livekit_v1_test.go` |
| an authored `endpointing_delay` reaches the floor on both targets and never the ceiling, and a package that authors nothing still emits the balanced floor and ceiling rather than inheriting a framework default | `internal/generate/endpointing_delay_test.go`, `internal/ir/validate_test.go` |
| a `pace` reaches the ceiling on both targets, every pace has a complete per-target row, no Pipecat row varies the floor, and `patient` reproduces the framework default | `internal/target/pace_test.go`, `internal/generate/endpointing_delay_test.go` |
| `pace` is refused on a non-turn binding, on an unknown value naming all three, and in a per-target override | `internal/ir/validate_test.go` (`TestValidatePace*`) |
| each emitted runbook names the floor and the ceiling separately, says whether the wait adapts, and on Pipecat distinguishes the two fields both spelled `stop_secs` | `internal/generate/turn_runbook_test.go` |
| `semantic_endpointing: off` removes the turn model on LiveKit and the end-of-turn analyzer on Pipecat, every other value keeps it, and the analyzer imports follow the branch | `internal/generate/semantic_endpointing_test.go` |
| a Pipecat package setting `interruption.minimum_words` keeps its end-of-turn analyzer and its ceiling, so the silent downgrade cannot come back | `internal/generate/semantic_endpointing_test.go` (`TestPipecatEmitsNoSpeechTimeoutUnlessAsked`) |
| a turn field set on the base binding survives a per-target `models:` override, which replaces rather than merges | `internal/generate/semantic_endpointing_test.go` (`TestTurnFieldsSurviveAPerTargetOverride`, over `examples/salon-concierge`) |
| a turn binding carrying `params`, `agent_id` or `fallback` warns that the field reaches nothing, and the same fields on any other role stay quiet | `internal/ir/validate_test.go` (`TestValidateWarnsOnTurnFieldsThatReachNothing`) |
| a router binding's forwardable params ride the request body on both targets and reach no constructor kwarg, while a folded field stays in the construction | `internal/generate/slng_router_test.go` (`TestSlngRouterForwardableParamsRideTheBody`, `TestSlngRouterFoldedFieldsStayInTheConstruction`) |
| an authored `prompt_suffix` reaches every agent and task prompt on its own think profile, no other profile's, and the LiveKit summarizer's; and it moves no cache scope | `internal/ir/build_test.go`, `internal/generate/slng_router_test.go` (`TestSlngRouterSummarizerCarriesThePromptDirective`, over `internal/testdata/summary_core`) |
| every `prompt_suffix` refusal names what to do instead, an off-router think binding warns rather than refusing, and a per-target override cannot name a second value | `internal/ir/validate_test.go` (`TestValidatePromptSuffix*`) |
| the `reasoning_effort` advice fires only on an upstream serving OpenAI's own models, because elsewhere the param is what the host refuses | `internal/ir/validate_test.go` (`TestValidateSlngRouterWarnsOnToolsWithoutReasoningEffort`) |
| the console's target menus lead with the right target: create with `scaffold.DefaultTarget`, maintain with the package's own | `internal/tui/default_target_test.go` |
| every telephony route deploys to a managed platform: every Pipecat route emits a deploy manifest on the platform base image, and no LiveKit route emits one | `internal/generate/cloud_isolation_test.go` |
| an inbound LiveKit SIP route emits the trunk and dispatch-rule records a call needs, and the one command that creates them | `internal/generate/livekit_telephony_setup_test.go` |
| a route that hosts nothing says so everywhere: no tunnel, helper or hosting word in its emitted runbook | `internal/generate/pipecat_cloud_websocket_test.go` |
| the Pipecat image copies every emitted module `bot.py` imports (its COPY lines are named, so a new module is a `ModuleNotFoundError` at startup) | `internal/generate/pipecat_image_imports_test.go` |
| a Pipecat phone route protects the greeting, an authored `protect` overrides it, `[]` suppresses it, and every mute class named is imported | `internal/generate/pipecat_user_mute_test.go` |
| the emitted transfer document and the plane's reading of it stay in agreement | `internal/generate` (emitted shape) + `internal/cli` (the reading), one gate each |
| the docs-site transfer table matches the route table, both directions | `internal/docsite/transfer_table_test.go` |
| the docs-site tool tables match the capability table on all three targets, and the page's own count of execution blocks is right | `internal/docsite/tool_table_test.go` |
| an L4 smoke fixture still compiles the salon package, the emitted Python keeps the names its script calls, and every name a smoke script monkeypatches still exists and is still called the way the stub expects | `internal/generate/smoke_fixture_test.go` (`TestSmokeStubbedNamesExistInTheEmittedModule`) |
| every attribute the emitted LiveKit modules read off a `ctx` is one the salon smokes' `run_context` stand-in carries, or is listed as a JobContext read: pinning the names a script *constructs* and *monkeypatches* left a third way to break an opt-in suite in silence, and `dev_task_finished` walked straight through it by reading `ctx.session` | `internal/generate/smoke_fixture_test.go` (`TestSmokeLiveKitRunContextStandInCarriesEveryAttributeRead`), whose stand-in lives in that untagged file so the gate reads it in the default suite |
| every emitted tool body tags its `config` with its own `tool_type`, because an update PATCH strips `tool_type` and an untagged body then deploys once and 422s forever after | `internal/generate/slng_v1_test.go` (`TestSlngV1ToolConfigCarriesItsUnionTag`) |
| every `voiceai agents push --json` shape decodes into the fields deploy reads, blockers relay every item, detail and dashboard page, an update warns that a push replaces, and a run that printed a problem exits non-zero | `internal/cli/deploy_test.go` |
| a tool sample written into `build/<target>/samples/` survives the next compile | `internal/cli/deploy_test.go` (`TestCompilePreservesToolSamples`) |
| both Langfuse targets send `x-langfuse-ingestion-version: 4`, which is the only thing that selects the v4 data model and the one thing a wrong export never reports: the spans still arrive, on the legacy path | `internal/generate/langfuse_v4_test.go` (`TestLangfuseSelectsTheV4IngestionPathOnBothTargets`) |
| no emitted module writes `langfuse.trace.input` or `langfuse.trace.output`, and both targets record each turn's pair on that turn's root observation instead | `internal/generate/langfuse_v4_test.go` (`TestLangfuseWritesNoDeprecatedTraceIO`), and per target in `internal/generate/livekit_v1_test.go` and `pipecat_v1_test.go` |
| the session ID and trace name reach every span, not only the top one, because v4 filters and sums over observations and neither framework does this itself: Pipecat's `additional_span_attributes` reach the conversation span alone, and livekit-agents 1.8.0 stopped honouring its `metadata=` inside a job | `internal/generate/langfuse_v4_test.go` (`TestLangfuseCorrelatingAttributesReachEverySpan`), proven at L4 by `TestSmokeV17PipecatSpeechTracing` asserting it on the conversation, turn, tool and generation spans |
| a call is **one trace**, whose root observation holds the conversation, with a `turn` span per exchange inside it carrying that exchange's two sides. One trace per turn was built and reverted on 2026-09-07 after reading real calls: the turns read well and the top of the call was an empty envelope, and a trace is the unit you aggregate over. LiveKit needs a `turn` span of its own, because `user_turn` closes before the reply exists and `agent_turn` is its sibling, and it patches the **provider** rather than a tracer object because livekit decorates `llm_node` at import time and a tracer patch misses it silently. Pipecat already nests `turn` under `conversation` and is left alone | `internal/generate/langfuse_v4_test.go` (`TestLangfuseKeepsTheWholeCallInOneTrace`), proven at L4 by `TestSmokeV22LiveKitSpeechTracing` and `TestSmokeV17PipecatSpeechTracing` |
| a Coval trace's correlation is per call, not per process, so a warm Pipecat Cloud container files its second call as its own conversation and never appends it to the first | `internal/generate/coval_tracing_test.go` (`TestCovalTracingResetsCorrelationPerCall`), proven at L4 by `TestSmokeCovalTracingPipecat`, which drives three sessions through one module and hand-resets nothing |
| a simulation still claims a call that is not the first its process handled, because the per-call reset clears the simulation ID and runs just before `activate_simulation` | `internal/generate/coval_tracing_smoke_test.go` (third session) |
| the OTLP exporter keeps Coval's required 30s while the shutdown-path call registration gets its own 5s, which fits LiveKit's 10s kill window, and a budget cut short is logged | `internal/generate/coval_tracing_test.go` (`TestCovalTracingSplitsTheSubmitBudgetFromTheExportTimeout`) |
| the local-run marker is one string across `generate.LocalRunEnv` and both tracing templates, a local run's Coval labels carry `-local`, a deployed run's do not, and both targets stay distinguishable | `internal/generate/coval_tracing_test.go` (`TestCovalTracingOwnsTheLocalRunMarker`, `TestCovalTracingMarksLocalRuns`, `TestCovalTracingLabelsEveryPlaceCovalFilters`) |
| `unmute dev` actually sets that marker, and a stale value in a `.env` cannot beat it | `internal/cli/dev_test.go` (`TestDevChildEnv_marksTheRunAsLocal`) |
| the agent logs one startup line naming provider, destination and whether a credential is present, one line per trace outcome, and no line claims a trace is not exported when the conversation route will export it | `internal/generate/coval_tracing_test.go` (`TestCovalTracingLogsWhatActuallyHappened`) |
| no docs-site page states a version, by a literal or by rendering the release automation's marker | `internal/docsite/version_test.go` |
| no docs-site page uses an em or en dash as punctuation outside a code fence, and none tells the reader how the author checked a fact ("we measured", "Measured on 20..", "Where this page's facts come from") | `internal/docsite/prose_test.go` |
| the docs-site declares its Markdown surface, and the coding-agents page names all three endpoints | `internal/skill/markdown_surface_test.go` |
| the changelog runs newest first, every entry carries a label, a version and its own release link, the newest matches the version snippet, and no entry keeps a heading, a dash or a commit hash | `internal/docsite/changelog_test.go` |
| the runbook's vault table names exactly the entries the preflight checks | `internal/generate/slng_requirements_test.go` (`TestSlngRequirementsIsWhatTheRunbookPrints`) |
| a `builtin:` reference is checked by its tool **file** name, because that is what the emitted ref carries, and a control is never checked because it emits no ref | `internal/cli/preflight_test.go`, `internal/generate/slng_requirements_test.go` |
| a code or webhook tool is never reported missing from the account, because the push creates it | `internal/cli/preflight_test.go` (`TestPreflightNeverReportsAToolThePushCreates`) |
| an account read that could not be made never counts as satisfied, and whether it also stops the run depends on what still covers the gap: a read the push re-checks itself, such as the vault, only warns and the run reaches the push | `internal/cli/preflight_test.go`, `internal/cli/deploy_test.go` (`TestDeployPreflightDegrades`) |
| an account read `unmute deploy`'s own promise depends on, a published tool version or an MCP schema hash, blocks instead of warning: the guarded push attaches exactly what this run resolved and checks none of it itself, so a read that failed here has no later step to catch it, and this used to warn and reach the push, which is how a deployment could report success having verified nothing | `internal/cli/deploy_test.go` (`TestDeployRefusesWhenAResolvedCheckCannotBeMade`) |
| an `inject:` override on a hosted reference is checked per property against the published schema and not as the tool's whole input, `false` and `0` are values and not missing ones, an argument the schema would let the model send but an attachment cannot fix is refused with the platform's own reason and never by claiming the schema forbids it, and a schema this check cannot evaluate reliably blocks rather than passing silently | `internal/cli/slng_bindings_test.go` |
| a deploy preview names an attachment's removals and every dashboard-only setting that goes with each one, an agent that could not be read is unknown rather than empty, and a changed published schema or description is reported as a fact and never as proof a tool's behaviour changed | `internal/cli/deploy_report_test.go` |
| a binding check carries the published root's `$defs` so a local reference resolves, refuses a reference it will not follow at **any** depth, and refuses a root constraint that reaches the supplied property rather than evaluating one written for the whole invocation. A pinned argument's destination must settle to exactly one non-null scalar type, which is the platform's own restriction and the one a schema validator cannot see; and a variable may land on a typed parameter, because provisioned text is converted rather than required to be text | `internal/cli/slng_bindings_test.go` (`TestBindingResolvesARootLocalDefinition`, `TestBindingRefusesAReferenceItCannotResolve`, `TestBindingRefusesARootConstraintOnTheSuppliedArgument`, `TestBindingRefusesAnAmbiguousDestination`, `TestBindingAcceptsAVariableIntoATypedParameter`) |
| two tool files resolving to one hosted tool are refused before anything is staged, naming both, because SLNG keys an attachment by tool id and a push would keep one file's settings and drop the other's in silence | `internal/cli/deploy_test.go` (`TestDeployRefusesTwoReferencesToOneHostedTool`); the caching case it used to share lives in `TestDeployReadsEachResourceOnce` |
| the preview reads the platform's own attachment shape, so a trigger under `system.triggers` and the arguments and per-attachment configuration beside it are named as things a replacement removes, on an MCP selection too; nothing being attached is also reported as detached, and no contract change is claimed without both contracts | `internal/cli/deploy_report_test.go` (`TestPreviewNeverCallsOneAttachmentBothKeptAndDetached`, `TestPreviewClaimsNoContractChangeWithoutBothContracts`, `TestPreviewNamesWhatAnMCPReplacementWouldRemove`) |
| a setting the package **can** declare is compared against what it proposes, never reported as a removal on sight: `announce:` compiles to the attachment's pre-action message, so an unchanged sentence prints no line and a different one prints the replacement; only the parts of that policy a package has no key for, `wait` and anything this preview does not read, are named as things a replacement clears. A real guarded dry run told an author that writing their own authored announcement would delete it | `internal/cli/deploy_report_test.go` (`TestPreviewComparesTheAnnouncementItWouldWrite`) and the whole-run half in `internal/cli/deploy_test.go` (`TestDeployReportsNoPhantomRemovalOfAnAuthoredAnnouncement`), both watched failing against a preview given no announcement to compare |
| the root-constraint walk follows the pointers a published schema keeps its shapes behind, at any depth, and treats a pointer it cannot follow as relevant: a root `allOf` branch is usually a `$ref` into `$defs`, and stopping at the pointer saw a branch naming no property, ruled it irrelevant, and reported a fixed value as checked against a constraint nobody read. A definition about another parameter is still not this argument's problem | `internal/cli/slng_bindings_test.go` (`TestBindingRefusesARootConstraintBehindAReference`) |
| a pinned argument's destination is resolved by narrowing, exactly the way the platform's `resolve_non_null_json_types` narrows: a node with no `type` starts from every type, and its own `type`, its definition, its `anyOf`/`oneOf` union and each `allOf` branch are all intersected, `null` dropped once at the end and the platform's `number`/`integer` exception carried. Returning the declared `type` and stopping refused `["string","integer"]` narrowed to integer, which the platform accepts; a declared type and a constraint with nothing in common reads as having nothing to pin rather than too much | `internal/cli/slng_bindings_test.go` (`TestBindingResolvesADestinationTheWayThePlatformDoes`) |
| an MCP snapshot with no observation time or no refresh schedule is **not** fresh: the platform expires one on its observation age against a TTL that is a server setting, `next_refresh_at` is background discovery at 80-90% of it and is read only as a conservative proxy, and each way a snapshot fails to be current reads differently rather than rendering an empty deadline | `internal/cli/preflight_test.go` (`TestMCPRecordStatesAreReadTogether`) |
| a blocked finding is printed whatever its `Kind`, because the kind order used to be the filter as well as the order: a finding of an unlisted kind was counted in "1 thing to fix" and printed nowhere | `internal/cli/preflight_test.go` (`TestEveryBlockedFindingIsPrinted`) |
| a vault entry whose kind the listing does not report is not a mismatch: a real `secret list` carries the name and `has_value` and no kind, and reading the absent field as `""` told the author to delete a correct populated entry. The shared stubs carry the real shape, which is what let this survive a green suite | `internal/cli/preflight_test.go` (`TestVaultEntryWithNoReportedKindIsNotAMismatch`) |
| a `--json` document that decodes as far as it goes and then stops is named as truncated, structurally, rather than reported as unreadable: a large document written to a pipe can be cut off when the tool exits before the write drains | `internal/cli/voiceai_test.go` (`TestVoiceaiNamesATruncatedDocument`) |
| `unmute pull` adds a credential name to `secrets:` only where a target builds the tool itself, because the slng driver reads that list nowhere and `unmute deploy` discovers the same requirement from the published contract | `internal/cli/pull_test.go` (`TestPullAddsNoSecretToAPackageThatOnlyDeploysToSlng`, and its code-target half `TestPullAddsASecretNameThePlatformOnlyNamesInItsConfig`) |
| an unreadable vault listing blocks a **discovered** credential and warns on a **declared** one, because the push's own vault check reads the package's prompts and its own tool bodies and nothing else: a name from a published contract or a hosted MCP server never reaches it. Neither run establishes whether an entry holds a value, and the warning says so rather than claiming the push repeats the check | `internal/cli/preflight_test.go` (`TestAnUnreadableVaultBlocksADiscoveredNeedAndWarnsOnADeclaredOne`) |
| an MCP record is healthy on the platform's own word and no alias of ours, because a permissive one can only pass something the push refuses; `capability_revision` is deliberately **not** compared, and the reason is inline: the platform's schema already constrains it to equal `revision` on any record we would call healthy, and the field is not one the CLI types, so an absent one would decode as zero and refuse every deploy using MCP | `internal/cli/preflight_test.go` (`TestMCPHealthIsThePlatformsOwnWord`) |
| what covers an unmade check is said **per requirement** and never as one blanket clause on the warning line: a failed `tool list` used to print "the push decides what it would have covered" four lines above a refusal for the references it could not resolve. A non-blocking unmade check is still printed, with its own reason | `internal/cli/preflight_test.go` (`TestPreflightTreatsAnUnmadeCheckAsNeitherPassNorFail`), `internal/cli/deploy_test.go` (`TestDeployPreflightDegrades`), both asserting the blanket claim is gone |
| a dry run makes no remote change of any kind, and an installed `voiceai` that cannot make a checked, resolved push is refused with upgrade guidance before anything is written rather than falling back to an unchecked one | `internal/cli/deploy_test.go` (`TestDryRunMakesNoRemoteChange`, `TestDeployRefusesAnIncompatiblePushTool`) |
| the preflight refuses before anything is written, `--dry-run` included | `internal/cli/deploy_test.go` (`TestDeployPreflightRefusesBeforeWritingAnything`, `TestDeployRunsThePreflightUnderDryRun`) |
| no secret value reaches argv, a written file, or either output stream | `internal/cli/preflight_test.go` (`TestFillNeverPutsAValueOnACommandLineOrInOutput`) |
| a secret create is the only write a deploy makes; no tool, MCP server or trunk is ever created, changed or deleted | `internal/cli/preflight_test.go` (`TestDeployWritesNothingButSecrets`) |
| each account resource kind is read at most once per deploy, the post-push trunk read included | `internal/cli/voiceai_test.go` (`TestVoiceaiReadsEachResourceKindOnce`) |
| every `voiceai` command any surface or constant names is one the CLI has | `internal/target/slng_target_test.go` (`TestEveryVoiceaiCommandNamedExists`) |
| attaching a trunk is a one-field PATCH on the agent, chosen by the operator, and never happens unasked or unattended | `internal/cli/deploy_test.go` (`TestAttachTrunkPatchesOneFieldByDirection`, `TestOfferTrunkAttachesNothingWhenDeclined`, `TestDeployAttachesTheChosenTrunk`) |
| the deployed agent name comes from the package, never read back from the push (which sends none), so a free trunk's empty `in_use_by` cannot match it and be reported as reaching the agent | `internal/cli/deploy_test.go` (`TestDeployNeverReadsTheAgentNameBackFromThePush`) |
| an `mcp:` package deploys to slng, and no surface says the push must reach the server | `internal/ir/validate_slng_test.go` (`TestSlngRequiresAnExplicitMCPToolList`), `internal/generate/slng_v1_test.go`, `internal/docsite/tool_table_test.go` |
| a `type:` outside the closed grammar is refused with its line **and its column**, naming what to write instead; the column is what makes a typo inside a nested expression findable | `internal/spec/typeexpr_test.go` (`TestParseTypeRefusesWithItsColumn`) and `internal/ir/shape_test.go` (`TestBuildRefusesEveryTypeOutsideTheScope`) |
| `list[Literal["a","b"]]` parses, and `go/parser` is not used for it: Go allows a multi-index only at the top level of an index expression, so it refuses the one shape this feature exists for | `internal/spec/typeexpr_test.go` (`TestParseTypeReadsTheGrammar`), with the rejection recorded as a comment in `typeexpr.go` so nobody retries it |
| a shape's field takes `name:`, `type:` and `description:` and nothing else, `confirm:` refused **by name**, because a per-field mark would let a guard on an unconfirmed value be escaped by nesting one level down | `internal/spec/field_test.go` (`TestFieldRefusesConfirmByName`) |
| a shape that refers to itself, one declared twice, one with no fields, one named after part of the grammar, an empty `Literal[]`, a `list[]`, a list of lists, a list that may be absent and a field declared twice are each refused, naming the file, the line and what to do | `internal/ir/shape_test.go` (`TestBuildRefusesEveryBrokenShapeDeclaration`) |
| a package declaring no shape and no structured type emits **byte-identical** output on both targets: no helper, class, import or list default | `internal/generate/typed_state_test.go` (`TestTypedStateEmitsNothingForAPackageThatDeclaresNone`) plus the golden files |
| a structured value renders as compact JSON and never a Python repr, the size bound is measured on the JSON, and a shortened value logs a warning carrying no `%s` and no `.format()` because the line is emitted into two modules that log through two different libraries | `internal/generate/state_block_test.go` (`TestStateBlockRendersJSONAndNotARepr`, `TestStateBlockWarningCarriesNoLibrarySpecificPlaceholder`) |
| every prompt naming a declared value reads as a whole sentence with that value empty, which is why an empty one renders as words rather than as `[]` | `internal/generate/state_block_test.go` (`TestStateBlockPromptsReadWholeWithEveryValueEmpty`) |
| no `format` and no `pattern` reaches an emitted schema, and every declared `description:` does: a shaped text type is `str` plus an `AfterValidator`, because one target's strict converter strips neither keyword and strict is its default | `internal/generate/typed_state_test.go` (`TestTypedStatePutsNoShapeKeywordInAnEmittedSchema`, `TestTypedStateCarriesEveryDeclaredDescription`) |
| LiveKit's finish parameter for a shaped result is the generated class, never a bare `dict`, which told the model nothing about the object it was asked for | `internal/generate/typed_state_test.go` (`TestLiveKitFinishParameterIsTheGeneratedClass`) |
| a value outside a declared `Literal` set is refused where it enters, the previous contents survive, and the message names the field and lists what was allowed; two appended entries are both recorded, in order | `internal/generate/typed_state_test.go` (`TestAValueOutsideALiteralSetIsRefusedWhereItEnters`, `TestTwoAppendedEntriesAreBothRecorded`), driven for real by `internal/generate/typed_state_smoke_test.go` |
| an append on a value that is not a list is refused naming its declared type | `internal/ir/shape_test.go` (`TestBuildRefusesAnAppendOnSomethingThatIsNotAList`) |
| Pipecat reads the router's template variables from live state on every request through `build_chat_completion_params`, emits no settings-frame refresh and no constructor snapshot, so a value written mid-turn is not one turn late | `internal/generate/slng_router_test.go` (`TestSlngRouterPipecatReadsTheVariablesPerRequest`, `TestSlngRouterSendsTheWholeBodyPerRequest`) |
| the declared-state Python is byte-identical on both targets, because it is rendered once above the drivers | `internal/generate/typed_state_test.go` (`TestTypedStateBlockIsByteIdenticalOnBothTargets`) |
| a package declaring `shapes:` round-trips through `unmute maintain` with no reported loss, because the console rewrites `agent.yaml` from `scaffold.Data` and a field that struct does not carry is a field it deletes | `internal/tui/typed_state_roundtrip_test.go` (`TestMaintainKeepsDeclaredShapes`) |
| any declared shape or shaped text type is refused on the slng target, naming the target and saying to declare a primitive or compile to a code target | `internal/ir/validate_slng_test.go` (`TestSlngRefusesADeclaredShape`, `TestSlngRefusesAShapedTextType`) |
| the hand-wired `TypeRef` schema publishes one property per struct field, because reflection cannot follow a type into itself and the derived debug schema would silently stop describing what the compiler produces | `internal/ir/shape_test.go` (`TestTypeRefSchemaPublishesEveryField`) |
| `internal/voice-agents-tests/salon-concierge-v2` keeps its three declared shapes, its typed values, the nested shape the `$defs` question needs and the two steps that append rather than replace; only the steps that **act** record why the caller rang, and the `Customer` record holds no id, because a shaped field no tool in the package can fill leaves the model inventing one or sending empty | `internal/generate/examples_test.go` (`TestSalonConciergeV2ScopesEveryStep`) |
| a shaped text value accepts the **empty string** and still refuses a wrong one, because empty is how a declared value says nothing yet: it can be a variable's `default:` or what a tool hands back for a field it could not fill | `internal/generate/typed_state_test.go` (`TestShapedTextAcceptsNoValueYet`) |
| a shaped text type tells the model its format, through the one keyword that can reach it: a `description` on the emitted alias, since a `format` or a `pattern` is stripped or refused. One phrase serves both it and the refusal, so the two cannot name different formats, and a field carrying its own description gets the phrase appended because Pydantic drops the alias's. A hand-written validator body carries the phrase through one token rather than repeating it. Left off, the format was learned from a refusal mid-call: a live call spent a model round trip sending `"11:30 AM"` for a `Time` while its own prompt requires that spelling out loud | `internal/generate/typed_state_test.go` (`TestShapedTextTellsTheModelItsFormat`), which loops `ir.ShapedTextOrder()` rather than a copy of it |
| every shaped kind carries **exactly one** check, a pattern or a whole validator body, because `shapedPatterns` is a map: a kind with no row reads a zero value and emits `re.compile(r"")`, which matches everything, with a refusal saying only "expected ". A package whose only shaped type is checked by a library compiles no pattern and must not import `re`, which the emitted project's own ruff gate refuses as unused | `internal/generate/typed_state_test.go` (`TestEveryShapedKindCarriesOneCheck`, `TestAnEmailOnlyPackageCompilesNoPattern`), both watched failing with the check removed and with both checks present |
| `EmailStr` and `NameEmail` are **this compiler's**, never Pydantic's: `pydantic.EmailStr` publishes `format: email` and `NameEmail` publishes `format: name-email`, the emitted `_schema()` strips no keyword, and the gate above greps the module's own source, so the Pydantic import would read clean locally and still send `format` to a provider that rejects it. Only the first real call would say so | `internal/generate/typed_state_test.go` (`TestNoEmittedModuleImportsPydanticsOwnEmailTypes`), which refuses either name in a `from pydantic import` line |
| the emitted email check never asks DNS: `email-validator` defaults `check_deliverability` to true, which sends MX queries, and this validator runs while the caller is on the line, so a slow resolver holds them in silence and a real address whose mail server is having a bad day is refused. Asserted per call site, because the module has two | `internal/generate/typed_state_test.go` (`TestTheEmittedEmailCheckNeverAsksDNS`) |
| the email checker's import, its use and its declared dependency agree on both targets, proven with a package that needs it beside one that does not: an import with no dependency is an ImportError at worker startup, a dependency with no import is a package every image installs for nothing, and an import with no use fails the ruff gate the emitted README promises | `internal/generate/typed_state_test.go` (`TestEmailValidatorImportMatchesItsUseAndItsDependency`), modelled on the httpx agreement in `pipecat_image_imports_test.go` |
| `NameEmail` is a shape the **compiler supplies**, seeded into the resolved catalog when a type names it, because `FieldPath`, `checkPathFields`, `checkAssignments`, the per-target rows and the class emitter all read `agent.Shapes` and nothing else: a reference with no entry there is told it has no fields to name, emits no class while the annotation still names one, and passes the slng gate that starts from whether the catalog is empty. Its emitted class also reads a single string, because a tool result and a dotted assign hand one over | `internal/ir/shape_test.go` (`TestBuildAssignAcceptsASubFieldOfASuppliedShape`), `internal/ir/path_test.go`, `internal/ir/validate_slng_test.go` (`TestSlngRefusesAnEmailType`), `internal/generate/typed_state_test.go` (`TestTheEmittedPairReadsOneString`) |
| every shaped type reaches every surface: the constants, the spelling an author writes, the refusal vocabulary, the derived schema's enum and the emitted aliases all read one ordered list. They were four hand-written lists, and a type missing from one resolves as an undeclared shape, stops being described in the debug schema, or is written into an annotation no module defines | `internal/ir/shape_test.go` (`TestEveryShapedTypeReachesEverySurface`), watched failing for each surface removed |
| every append onto a declared list goes through one emitted helper, which drops an absent or duplicate structured entry; a plain value still appends every time | `internal/generate/typed_state_test.go` (`TestAnAbsentEntryAppendsNothing`, `TestTwoAppendedEntriesAreBothRecorded`) |
| a declared result field the model left out validates as `None` rather than vanishing from the result, because the assignment that reads it by name raised a `KeyError` inside the finish handler on the target whose framework validates no tool argument of its own. A field that may not be absent is still refused, with the message that lets the model correct itself | `internal/generate/typed_state_test.go` (`TestAnOmittedResultFieldValidatesRatherThanVanishing`) |
| no authored prompt writes a specimen email address either, for the same reason and with the same consequence: a caller who agrees to a stranger's address sends the confirmation to the stranger. An address is more tempting to illustrate than a number, because its shape is harder to describe in a sentence, and the type's own description already carries the format to the model | `internal/generate/examples_test.go` (`TestNoPromptCarriesASpecimenEmailAddress`), verified by writing one in and watching it fail |
| an address read back to the caller is **delimited**, not spelled out in prose: the skill's own prompting page says a speech engine normalizes `user@example.com` better than a prompt can, and the one exception, a value the caller has to hear character by character, is written by spacing the characters. Three authored prompts said "letter by letter" instead, which is the form that page removed from itself in 2026-08 after it lost on a shipped package | `internal/skill/assets/references/prompting.md` is the rule; the prompts are read by `TestSalonConciergePlaceholdersAgreeWithItsVariables` and the example gates (advisory: no test reads an instruction's phrasing) |
| no authored prompt writes a specimen phone number, because a model cannot tell an illustration from a value it is holding and reads it out: the concierge, whose own prompt says never to say the caller's number and carries no placeholder for one, opened a live call with the example number from its own formatting rule, and the caller said yes to a number that was not theirs. This is the other half of the `confirm:` refusal, which a hardcoded number walks straight past. Describe the grouping in words | `internal/generate/examples_test.go` (`TestNoPromptCarriesASpecimenPhoneNumber`), verified by reintroducing one and watching it fail |
| the owner rule for `unserved_request` says the same flow may be run again for it, because the delegate docstring also says not to run the flow again for the same request: an unserved booking request is servable only by the booking flow, and a live call resolved the contradiction by apologising to the caller | the golden files under `internal/generate/testdata/golden/` carry the rule verbatim on both targets |
| one scripted conversation drives both emitted modules and ends with identical declared state, field for field | `internal/generate/typed_state_smoke_test.go`, opt-in with `make smoke` |
| an agent holding every tool of one of its own steps is warned, naming the tool to move, because the model takes the short route and the step's `assign:` never runs: a specialist recorded a complaint itself and `complaints` stayed empty on a call that recorded one, with its own prompt saying to run the step. A warning and not a refusal, because whether the short route is taken is decided by the prompt and the `when:`, neither of which the compiler reads | `internal/ir/step_reach_test.go` (`TestWarnsWhenAnAgentHoldsEveryToolOfItsStep`) |
| an agent's prompt is re-rendered after one of its own steps writes state, so its explicit placeholders read the saved values on the next request | `internal/generate/state_refresh_test.go` (`TestLiveKitRefreshesTheOwnerPromptAfterAStepWritesState`, `TestBothTargetsRefreshDeclaredStateWithoutWaitingForAnEntry`) |
| a shape declares no field without a tool that fills it: `Customer` is two fields because the lookup returns two, and both an `id` and a name were removed after each failed on a live call, one by being invented and one by rendering empty in every prompt for the rest of the call | `internal/generate/examples_test.go` (`TestSalonConciergeV2ScopesEveryStep`), whose field floor is per shape with the reason inline |
| authored task `result:`, task or handoff `expect:`, and `requires:` are refused with their source line and migration advice, while a tool schema may still name ordinary `result` and `expect` properties | `internal/spec/input_test.go` (`TestRetiredTaskFieldsGiveLocatedAdvice`, `TestToolSchemaMayNameResultAndExpect`) |
| a task's finish fields and descriptions derive from its assignment destinations, and invalid multi-field saves change nothing | `internal/ir/shape_test.go` (`TestTaskResultDerivesFromAssignments`, `TestTaskResultDerivationPaths`), `internal/generate/typed_state_smoke_test.go` |
| only authored prompt references reach generated prompts and router scopes; a dotted path does not share its siblings | `internal/generate/state_refresh_test.go` (`TestPromptsContainOnlyAuthoredStateReferences`), `internal/generate/slng_router_test.go` (`TestRouterPathsBelongToActiveScope`) |
| task return restores the owner's context and adds only a completed or unserved status, never raw result fields, private task turns or unserved text | `internal/generate/state_refresh_test.go` (`TestTaskReturnsOnlyNeutralStatus`), `internal/generate/pipecat_v1_test.go` (`TestPipecatRestoresTheOwnerContextAtEveryHistoryValue`) |
| omitted task and handoff history is `messages`; `messages` keeps spoken turns without orphaned tool records, `reset` keeps no earlier conversation, and `full` keeps complete tool-call pairs | `internal/generate/livekit_v1_test.go` (`TestLiveKitV1HistoryShapingAndFallback`, `TestLiveKitV1HistoryResetAndToolCallShaping`), `internal/generate/pipecat_v1_test.go` (`TestPipecatMessagesLeavesNoOrphanedToolCall`, `TestPipecatShapesAHandoffByTheSameValues`) |
| `assign:` takes one part of a structured result through a dotted path into the declared shapes, such as `result.appointment.scheduled_date`; a path through a list, into a field with no declared shape, or to a field the shape does not declare is refused naming what does resolve, and a link that may be absent makes the picked value `| None`, so the variable is declared to match or the assign appends | `internal/ir/shape_test.go` (the dotted assign cases), `internal/generate/typed_state_test.go` (`TestDottedAssignWalksIntoAShapedResultAtEmission`), which asserts the emitted walk reads `None` past an absent or null link on both targets |
| any agent prompt and any task prompt may name any declared variable, written so the sentence reads whole when the value is empty; only the greeting is held to a value settled before the first word | `internal/ir/variables_test.go` (`TestTaskPromptMayNameAVariableOnlyAnotherStepAssigns` and the agent-prompt case) |
| a bare primitive holding `None` renders as nothing in a prompt, never the word `None`, on both targets | `internal/generate/state_block_test.go` (`TestStateTextRendersEmptyNotNoneForABarePrimitive`) |
| a prompt placeholder may name one part of a structured value through a dotted path, `{{customer.status}}`; the root follows every whole-value rule (undeclared, secret, `confirm:`, greeting), and invalid paths are refused with useful advice | `internal/ir/path_test.go` (`TestBuildResolvesAPlaceholderPath`) |
| a path is stored and emitted as one flat name, `{{customer__status}}`, because the SLNG router and the emitted regex substitute flat identifiers only; both targets walk it through one emitted helper that reads `None` past an absent link and hands the root's name to `_state_text` so a missing part renders the root's empty words; a single-token inject of a path keeps the part's own type and the unset guard names the root; a package naming no path emits no helper and no `__` | `internal/generate/placeholder_path_test.go`, `internal/ir/path_test.go` (`TestFlattenPathsAndPathRoot`), `internal/generate/typed_state_test.go` (`TestTypedStateEmitsNothingForAPackageThatDeclaresNone`, marker `def _state_lookup(`) |
| a shape's field name follows the variable name grammar, one underscore at a time, so the flat form of a path can never read as a deeper path | `internal/ir/shape_test.go` (`TestBuildRefusesEveryBrokenShapeDeclaration`) |
| a `source: conversation` variable is a value the model records mid-call: on slng it is emitted as a runtime variable and kept out of both template maps, because SLNG refuses a name that is both; it takes no `default:` and needs a `description:`, which the model reads; the code targets refuse it naming a task's `assign:` | `internal/ir/memory_variable_test.go`, `internal/generate/slng_memory_sms_test.go` (`TestSlngEmitsAConversationVariableAsARuntimeVariable`) |
| `builtin: send_sms` compiles on slng alone, a code target refuses the id by name, and its `inject:` holds exactly one `from_number` that is a literal E.164 number, because SLNG requires the sender on the attachment and the model supplies recipient and body; the driver writes it as `config_overrides` with the union tag and leaves `argument_overrides` empty, the deploy binding check skips it, and the preflight checks `TWILIO_ACCOUNT_SID` and `TWILIO_AUTH_TOKEN`, which the platform reads by those names | `internal/ir/memory_variable_test.go` (`TestBuiltinSendSmsSenderRules`, `TestCodeTargetsRefuseSendSmsByName`, `TestBuildRefusesInjectOnEndCall`), `internal/generate/slng_memory_sms_test.go`, `internal/target/prebuilt_agreement_test.go` |
| the deploy preview compares a sender the package pins rather than calling every `config_overrides` a setting the package cannot declare | `internal/cli/deploy_report_test.go` (`TestPreviewComparesTheSenderItWouldWrite`) |
| a package writing none of `finish:`, `skip_when_confirmed:` or `opening:` compiles to the bytes it compiled to before spec 010, on every code target it declares, and the one door out is a named allowlist carrying a reason per package | `internal/generate/compat_test.go` (`TestPackagesWritingNoNewKeyEmitTheSameBytes`), whose digests live in `internal/generate/testdata/compat_bytes.txt` |
| a `finish:` naming a tool the task does not hold, a success field the tool declares no `enum:` for, a value outside that enum, a result field a listed tool does not return, or an `object` or `array` output landing on a plain destination, is refused with the file, the line and what to write instead; the last one used to pass, because an output was typed as text unless the schema said number or boolean, and surfaced only after the business tool had run | `internal/ir/finish_test.go` (`TestBuildRefusesEveryBrokenFinishDeclaration`) |
| the published authoring schema accepts what the decoder accepts, held on whole shipped packages and their tool files: a `- status: booked` success item and a bare task name under a group's `steps:` are decoded by hand, and reflection published neither, so every package with a group failed the schema | `internal/spec/published_schema_test.go` (`TestPublishedSchemaAcceptsWhatTheDecoderAccepts`, `TestPublishedSchemaRefusesASuccessItemHoldingTwoKeys`), watched failing with each hook removed |
| a step that ends on its own tool closes that step's mutations **before** the handler runs, because a second booking that ran and was then refused is a second booking; the closing is **per step**, because on Pipecat it was held on the flow and the lookup that ended verification refused the caller's first booking as a second one, twice on live calls; and a refused save keeps the tool's result in front of the model with the instruction that recovers it | `internal/generate/terminal_test.go` (`TestASucceededMutationIsClosedForTheRestOfTheInvocation`, `TestARefusedSaveKeepsTheResultAndSaysHowToRepair`), driven for real by `TestSmokeTerminalOutcomes{LiveKit,Pipecat}` and by the salon's Pipecat journeys, which enter the real delegate and call the handlers its nodes register |
| a step that ends on its tool returns **nothing**, on the last step too, and the owner's reply when the delegate returns is the one acknowledgement: returning a status made the step speak and the owner speak again, one request and one sentence more than the caller needed, on a live call; and a repair after a refused save puts the tool's own values back before it saves, on both targets, so the model cannot manufacture a reference | `internal/generate/terminal_lifecycle_smoke_test.go` (`TestSmokeTerminalLifecycleLiveKit`), which drives a real AgentSession through two terminal steps, with and without the caller interrupting, and counts requests and spoken lines; `internal/generate/terminal_test.go` for the retained merge on both targets |
| a step that ends on its tool saves **once**: the wrapper hands its saved values to the shared advance rather than to a finish handler that saves again, because a second save is a second entry on every list an `assign:` appends to | `internal/generate/terminal_test.go` (`TestTerminalToolEndsTheStepOnBothTargets`), the save counted in `TestSmokeTerminalOutcomes{LiveKit,Pipecat}` |
| `skip_when_confirmed:` names a variable the skipped step itself confirms; a variable nobody confirms and one another step confirms are both refused, naming the confirming step | `internal/ir/finish_test.go` (`TestBuildRefusesASkipOnAnUnconfirmedVariable`, `TestBuildRefusesASkipOnAnotherTasksConfirmation`) |
| entering a step some group may skip withdraws what it confirms, before its prompt renders, and every value derived from a withdrawn one follows | `internal/generate/group_skip_test.go` (`TestAWithdrawingTaskUnconfirmsOnEntry`, `TestWithdrawalClearsDerivedValuesToo`) |
| a group stops when a step ends unserved, shared or isolated, and a handoff or a finish arriving while a tool that ends the step is running, in the same response or a later one, waits for it to settle and moves the caller only on a committed success; a failed action settles too, so the wait ends when the action does rather than at the timeout | `internal/generate/group_skip_test.go` (`TestAGroupStopsOnUnserved`, `TestAHandoffWaitsForATerminalCallToSettle`), `internal/generate/livekit_v1_test.go` (`TestLiveKitV1IsolatedGroup`), driven by `TestSmokeGroupExitsLiveKit` for the same-response and later-response branches and by `TestSmokeTerminalLifecycleLiveKit` for the isolated stop on the real session |
| the caller turn a terminal step carries back to the owner is decided by counting the caller's turns against the owner's snapshot, never by matching text, because a caller who says the same word twice has said it twice | `internal/generate/terminal_test.go` (`TestTheCarriedTurnReachesTheOwnerOnce`), and the salon's Pipecat journeys assert the turn appears once |
| the salon's booking backend refuses a second `create_booking` while the caller holds one unless `additional` is set, and refuses a slot earlier than the salon's own clock today, because a live call answered "move it" with a second booking and offered 15:00 at four minutes past three, prompt notwithstanding | `examples/salon-concierge/tools/salon.py` (`_demo`, run by the salon smokes' store prelude), and both salon journeys smokes drive the refusal through the emitted tools |
| `opening: listen` speaks its line and makes no request, the line survives the step's own `history: reset`, and a step that writes nothing opens exactly as it did | `internal/generate/opening_test.go`, proven against the framework by `TestSmokeListeningOpening`, which reads the node Pipecat builds |
| a task's `finish:`, its `opening:` and a group step's `skip_when_confirmed:` survive a console round-trip, because a key `scaffold.Data` does not carry is a key `unmute maintain` deletes at exit 0 | `internal/tui/agent_name_roundtrip_test.go` (`TestMaintainKeepsATasksFinish`, `TestMaintainKeepsAGroupStepSkip`) |
| all three keys are refused on the slng target, each naming what it is and what to do instead | `internal/ir/validate_slng_test.go` (`TestSlngRefusesFinish`, `TestSlngRefusesAGroupSkip`, `TestSlngRefusesAListeningOpening`) |
| `turn: provider: listen` hands the turn to the transcriber on Pipecat and nowhere else: only a Deepgram Flux or Cartesia Turns listener can take it, the class is chosen from `internal/target/turn_listener.go` and the model family is checked because the class is loaded by it, the pace ceiling lands in the service's own timeout, `eager` is legal only there, and `endpointing_delay`, `semantic_endpointing` and `minimum_words` are refused because nothing under a listening decider reads them; a package keeping `provider: local` emits the same `bot.py`, and the docs-site table reads the Go table | `internal/ir/validate_turn_listener_test.go`, `internal/generate/turn_listener_test.go`, `internal/target/turn_listener_test.go`, `internal/docsite/turn_decider_table_test.go`, proven at L4 by `TestSmokeListenerDecidesTheTurn` |
| the emitted Pipecat bot takes neither shape Pipecat 1.9.0 deprecates for removal in 2.0.0: no module seeds `LLMContext` with a `"role": "system"` message (the prompt lives on the service as `system_instruction` and moves through `LLMUpdateSettingsFrame`; tracing.py's copy for the trace viewer is the one allowed spelling), and none calls `LatencyBreakdown.chronological_events()`. Both would print a framework warning on every call | `internal/generate/pipecat_deprecations_test.go` (`TestEmittedPipecatBotAvoidsThe190Deprecations`) |
| checked-in Python is clean | CI `python`, `ruff check .` |
| a mirrored SLNG module carries a three-line `# ruff: noqa` header, which is the only lint suppression in this tree: the platform's own code fails this repository's lint (`E402`, inside a shim unmute itself generated), and CI lints every tracked `.py`. Verified both ways rather than assumed | CI `python`; the header lives in `internal/spec/mirror.go` with the reason inline |
| no known-vulnerable dependency or stdlib | CI `vuln`, `govulncheck ./...` |
| release config still builds 6 platforms with version stamps | CI `release-config` |
| emitted Python is valid | `make smoke`, opt-in, never the PR gate |
| the vendored SLNG conformance fixtures still match what the backend publishes | `make contracts`, opt-in, needs network, never the PR gate |

A note on the third gate, because it looks redundant next to the second and is
not. `deadcode` walks the call graph and cannot see through `text/template`, so
a method called only from a template reads as dead to it, to `go vet`, to the
linter and to the compiler. Deleting one produces a green build and a broken
`unmute init`. An over-engineering audit of this tree proposed deleting six of
them at once. The two gates cover different things; neither replaces the other.

Ratchet only: a gate that starts failing gets the **code** fixed, not the gate loosened. A disabled check carries its reason inline, the way `.golangci.yml` explains every `errcheck` exclusion and every `forbidigo` pattern, and the single `//nolint` in the tree explains itself at the call site in `main.go`. One rule is still advisory: `ty check` over the examples, because every finding it has today is an unresolved `pipecat`/`livekit` import, and installing those SDKs is `make smoke`'s job.

## Four places document emitted behaviour
The generated `build/<target>/README.md` is the runbook, and almost nobody reads it before they have already read the example's page or the public docs. So **a change to emitted behaviour updates every surface it reaches in the same commit**:
1. the emitted README template,
2. the source example's own `README.md` under `examples/`,
3. the relevant page in `docs-site/`, which is the public answer a reader lands on,
4. **the skill** in `internal/skill/assets/`, which is what a coding assistant reads before it writes a package.

A fact that is only true in generated output is a fact the reader never sees, and a feature the skill does not know about is a feature no coding agent will use. The reverse rots too, and now fails: `internal/docsite/retired_output_test.go` refuses a page, a skill reference or an example README that quotes a line the CLI has stopped printing, because a reader who copies a stale sample and waits for it cannot tell a stale doc from a broken install. `docs/ARCHITECTURE.md` changes only when a system boundary, compiler stage, or runtime topology changes. Tests hold the parts that can be held: `internal/generate/examples_test.go` (example routes and links), `internal/skill/agreement_test.go` (the skill's factual lists), and `internal/cli/skill_bundle_test.go` (the commands and flags the skill names). Prose can still rot, so read the example page before you claim you are done.

## Layout
`internal/` not `pkg/`. One file per command in `internal/cli/`. Hand-write cobra commands — **no `cobra-cli` generator**.

### Three places hold a package, for three different reasons
`examples/` is what a reader is sent to, so it carries every example gate: a
README naming its transports, resolving links, the model and framework pins, and
a hardcoded set in `internal/generate/examples_test.go` that a new one has to
join deliberately. `internal/testdata/` is the smallest package that makes one
unit assertion possible. `internal/voice-agents-tests/` is a whole agent we
compile, deploy and talk to, to find what only a real call finds: not shipped,
nobody pointed at it, and held to one bar (validates clean and generates on every
target it declares) so a test agent that stops running fails the suite. Putting a
test agent in `examples/` is what this split exists to stop, because it makes the
public set grow with work nobody outside is meant to read. Two of those agents
are a pair: `salon-concierge-v2` is frozen at the state that closed typed
session state and is the control, and `salon-concierge-v3` exercises explicit
saved-value sharing across reset tasks, so a regression is measured against a
package known to work.

## Subagent-driven development
For complex or long-running tasks, use subagents by default when the work can be split into independent, bounded subtasks.

- Keep the main agent focused on requirements, decisions, coordination, and final integration.
- Delegate codebase exploration, documentation research, test and log analysis, reviews, and non-overlapping implementation.
- Run independent read-only tasks in parallel.
- Give each subagent a concrete scope and require a concise summary with file references, findings, and verification results.
- Wait for all required subagents before integrating their work.
- Avoid parallel edits to the same files or tightly coupled code paths.
- Handle small or inherently sequential tasks directly.
- If a suitable long-running task is not delegated, briefly state why.

## Skills
- Ponytail for writing great code
- GitHub Spec Kit for SDD (spec driven development) on any feature work: `/speckit-specify` → local `specs/<nnn>-<slug>/spec.md`, then `/speckit-plan`, `/speckit-tasks`, `/speckit-implement`. `specs/` is ignored. `.worktreeinclude` copies the local snapshot into new Codex-managed worktrees; copies do not synchronize afterward.
- find-docs skill to use context7 cli for searching docs
- The Coval skills for verifying runtime behaviour: `coval-resources` for the resource model, `configure-metrics` for writing a metric, `build-test-suite` and `distill-test-set` for scenarios, `setup-agent` + `launch-run`/`watch-run`/`get-results` (or `quick-eval`) for the dial-in path, `diagnose` and `debug-traces` for reading a failure. The `coval` CLI those last ones assume is not installed here, so the REST calls in [`docs/SELF_VERIFY.md`](docs/SELF_VERIFY.md) are the working path and the skills are the reference for what to call. Upstream: <https://github.com/coval-ai/coval-external-skills>.
