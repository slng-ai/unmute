description: Read the clock, so you know what day it is before any time is discussed.

builtin:
  id: current_datetime

# The capability's own setting, not an argument: the curated tool defaults to
# UTC and belongs to no organisation, so the zone is pinned on the attachment.
inject:
  - timezone: "America/Los_Angeles"

effect: returns_data
