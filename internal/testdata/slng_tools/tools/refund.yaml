# A hosted `api_request` tool. It replaces the `webhook:` tool that used to be
# here, and with it goes the templated path that was the only reason
# config_overrides existed on an attachment: SLNG stores a hosted tool's whole
# URL, and unmute writes no tool body to template.
slng:
  hash: 5600c5ecacce4f5bbfa4d89516b1aafe03645f3a24b7e3ddd705bf1761ef78db

inject:
  channel: voice

interruption: provider_default
effect: returns_data
