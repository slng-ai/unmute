# A tool SLNG hosts, with dependencies. That combination is the point of this
# one: it compiles to slng, which installs a per-tool environment, and is
# refused on livekit and pipecat, which build one dependency list for the whole
# project. The mirror is synthesised; see the sidecar's own note.
description: Look up an order by its id and return its status and delivery date.

slng:
  hash: 2d291d39d704a38afa60555d2f6e27e40c6074ad60407f301c14d0a11d2c2efd

announce: One moment while I look that up.
interruption: provider_default
effect: returns_data
