# Starter keys for the selected target. Copy this file to `.env` next to it,
# fill in the values, then run `unmute dev .` from this directory. `.env` is
# never committed. `unmute compile .` writes an exact per-target
# build/<target>/.env.example from your spec.
[[range .RequiredEnv]][[.]]=
[[end -]]
