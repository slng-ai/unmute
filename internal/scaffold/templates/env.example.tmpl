# Starter keys for the default target. Copy to `.env`, fill in, then run
# `unmute dev [[.Name]]`. `unmute compile` writes an exact per-target
# build/<target>/.env.example from your spec.
SLNG_API_KEY=
OPENAI_API_KEY=
