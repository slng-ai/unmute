# Your filled-in values. `.env.example` is the template and is safe to commit;
# this is the one with the keys in it.
.env
.env.*
!.env.example

# `unmute compile` output. Regenerated from the files beside it, so there is
# nothing here worth keeping in history.
build/
