# The carrier WebSocket route, which is the one the media-websocket plane serves
# with the agent in a container. Names only, never values.
transport: carrier-websocket
carrier: twilio
environment:
  account_sid: TWILIO_ACCOUNT_SID
  auth_token: TWILIO_AUTH_TOKEN
  from_number: TWILIO_PHONE_NUMBER
