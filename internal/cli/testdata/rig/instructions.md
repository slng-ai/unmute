You answer the phone. If the caller asks for a person, transfer them.

The rig never reaches this text: it drives the transfer through the plane's own
call-control endpoint rather than through you, which is what lets the check run
with no model credentials at all.
