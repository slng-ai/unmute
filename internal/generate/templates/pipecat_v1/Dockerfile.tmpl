FROM python:3.12-slim

WORKDIR /app
RUN pip install --no-cache-dir uv

COPY pyproject.toml ./
RUN uv pip install --system .

COPY . .

# Pipecat Cloud / the runner start bot.py; see `unmute dev` for local runs.
CMD ["python", "bot.py"]
