# Environment for {{.Project}} (generated by unmute).
# Copy this file to `.env` and fill in the values. Never commit `.env`.
{{range .RequiredEnv}}{{.}}=
{{end}}
# Optional Langfuse tracing. Set all three or leave all three empty.
LANGFUSE_SECRET_KEY=
LANGFUSE_PUBLIC_KEY=
LANGFUSE_BASE_URL=
