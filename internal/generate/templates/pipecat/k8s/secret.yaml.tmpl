apiVersion: v1
kind: Secret
metadata:
  name: {{.Profile.Kubernetes.SecretName}}
  namespace: {{.Profile.Kubernetes.Namespace}}
type: Opaque
# Values intentionally omitted. Create or sync this Secret from your secret
# store with these keys:
{{range .RequiredSecrets}}# - {{.}}
{{end}}
