[project]
name = "{{.Profile.PCC.AgentName}}"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "pipecat-ai[daily,openai,runner,silero,webrtc]>=1.4.0",
  "pipecat-slng",
  "python-dotenv>=1.0.0",
]
