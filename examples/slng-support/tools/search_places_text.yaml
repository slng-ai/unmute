slng: search_places_text

# The caller's name fills the query, so the model does not have to repeat back
# something it was already told. Every other argument the tool declares is left
# to the model.
inject:
  - query: "{{customer_name}}"

effect: returns_data
