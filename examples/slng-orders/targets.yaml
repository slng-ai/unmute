# One slng target. No version, no pins, no sdk_language and no connection: all
# four describe a project this target does not emit, and all four are refused by
# name rather than ignored.

targets:
  slng:
    provider: slng
    deployment_region: any
