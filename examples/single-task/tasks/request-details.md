# Collect request details

Collect only the information needed to return a structured support request.

1. Ask for a one-sentence description of the problem.
2. Ask whether work is blocked now.
3. Return `summary` as one sentence and `urgency` as `normal` or `urgent`.
