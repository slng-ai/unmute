description: >-
  List the verified customer's active bookings. The customer ID comes from
  verification and is never supplied or guessed by the model.

input:
  type: object
  properties: {}

inject:
  customer_id: "{{customer_id}}"

output:
  type: object
  properties:
    bookings:
      type: array
      items:
        type: object
        properties:
          booking_id:
            type: string
          service:
            type: string
          start_time:
            type: string
          status:
            type: string
        required:
          - booking_id
          - service
          - start_time
          - status
  required:
    - bookings

local:
  handler: tools/salon.py

interruption: provider_default
effect: returns_data
