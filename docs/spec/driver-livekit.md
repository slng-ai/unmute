# SPEC — LiveKit driver (generate.Artifact case)

Consumes the core: [compiler.md](compiler.md). Schema truth: [SCHEMA.md](../SCHEMA.md). LiveKit facts: SCHEMA.md conditions ⁰ + ORCHESTRATOR footnote ⁰. SCHEMA.md wins on disagreement.

Type: **code target**. Emits a LiveKit Agents project on disk (`Artifact.Files`). No generator today. SDK language matters: `sdk_language: python` is required for warm transfer and MCP.

## §G goal
Lower a validated `ir.Agent` + a `livekit` target instance into a runnable LiveKit Agents project: entrypoint (worker/session), agents as `Agent`s with handoff, tasks as `AgentTask`, task groups as `TaskGroup` or a standalone-`AgentTask` sequence, controls, tools, conversation, dependency/deploy files, compile report. The session resolves `listen`/`speak`/`reason` through the provider catalogue (`internal/target/catalog_livekit.go`): SLNG (`livekit-plugins-slng`) is the scaffold default, and any catalogued per-vendor plugin binds when the user picks it; a reason vendor without a native entry falls to LiveKit Inference, turn detection lowers to Inference with the version from the `turn:` binding, VAD to Silero, optional input noise cancellation to ai-coustics (C8, V17–V19). The `unmute init` example for LiveKit is an invented handoff + task-group agent bound to SLNG, so a first generation is a real SLNG agent, not a random plugin pick (V11, V12).

## §C constraints
- C1: Go emits the project via `text/template` (ADR-0002); embedded FS. Default SDK language `python`; `node` allowed. MCP is Python-only (V9); warm transfer works on **both** languages — stable on Node, Beta on Python (V6). (review-corrected 2026-07-15.)
- C2: all four roles are **open**: every used profile needs a binding (V9 core).
- C3: `TaskGroup` is experimental **and always shares context** → `context_scope: isolated` cannot lower to it; it lowers to a generated sequence of standalone `AgentTask`s instead. `TaskGroup` is itself an `AgentTask`, so on completion LiveKit merges its turns into the owner's context on handoff; `summarize_chat_ctx=True` summarizes them first. Setting `summarize_chat_ctx=False` skips the summarization but NOT the merge (verified against `voice/agent.py` 2026-07-15), so the `merge: results` lowering MUST also snapshot the owner context before awaiting the group and restore it afterward, returning only the typed results (N13). It still passes `summarize_chat_ctx=False` to skip the wasteful summarization call.
- C4: a standalone `AgentTask` starts with an empty chat context and does not propagate its turns back — the typed result is the only return (N13). This is the default (a `preserveFunctionCallHistory` option can override it; the templates must never set it). `reset` history still lands an `AgentHandoff` marker (Node: `AgentHandoffItem`) in the new context.
- C7: version pins are **per-SDK-language** — Python (`livekit-agents`) and Node (`@livekit/agents`) version independently and their namespaces diverge (`beta.workflows` vs stable `workflows`); the template-compatible range is checked per language.
- C5: full turn-detector model is a Cloud/Inference feature with automatic fallback to the mini model → the turn binding is a preference; forward + warn, never promise (SCHEMA N15: placement rides the binding).
- C6: version pins — LiveKit plugins are independently versioned and each needs a `pins:` entry; the driver checks pins against its template-compatible range (codegen check).
- C8 (amended 2026-07-18 with B4/B6; previously 2026-07-15, catalogue pilot): SLNG is the **scaffold default, not the only route**. `listen`/`speak` resolve through the provider catalogue: `provider: slng` lowers to `slng.STT`/`slng.TTS` from `livekit-plugins-slng`, the bound route passed to the plugin's `model=` argument **verbatim** — the SLNG API serves two route families, proxied (`deepgram/nova:3`) and SLNG-hosted (`slng/deepgram/nova:3-en`), the `slng/` prefix is part of the hosted URL path and the plugin interpolates `model=` into `/v1/{stt,tts}/{route}` unchanged, so stripping it 404s (B4; the pipecat entry always passed routes verbatim); any other catalogued vendor lowers to its per-vendor plugin (deepgram, elevenlabs, cartesia today, extras on the `livekit-agents` pin); an uncatalogued vendor fails with the matrix diagnostic, never a substitute service. `reason` binds a catalogued native LLM plugin when the vendor has one (openai included, B6 — the scaffold default must run `console` with no LiveKit Cloud creds); vendors without a native entry fall to the Inference wildcard, and `provider: livekit` is the deliberate Inference spelling (model verbatim in the `openai/gpt-4o-mini` form, billed through LiveKit Cloud). Turn detection lowers to `inference.TurnDetector` with the version from the target's `turn:` binding (V18), VAD to `silero.VAD`, optional input noise cancellation to `ai_coustics`. Reference: the working `taskgroups`/`handoffs` variants at github.com/slng-ai/livekit_orchestration_examples; the SLNG plugin is pinned per C6/T7 (`livekit-plugins-slng`).

## §I surfaces
- I.emit: `GenerateLiveKit(agent *ir.Agent, target ir.Target) (Artifact, error)` — the `livekit` case of `generate.Artifact`.
- I.templates: `templates/livekit/*.tmpl` (embedded) → entrypoint, agents, tasks, tools, deploy files, compile report.

## §V invariants
- V1: `task` lowers to a native `AgentTask` (empty context, no turn propagation, typed result only, C4).
- V2: `task_group` with `context_scope: shared` → `TaskGroup` with `summarize_chat_ctx=False` (C3) and an experimental **warning on Python** (stable in Node's `workflows` namespace — no warning there); with `context_scope: isolated` → a generated standalone-`AgentTask` sequence (no TaskGroup).
- V3: `then: return`/`transfer`/`end` all compile; `agent_transfer` is native handoff; `reset` keeps the `AgentHandoff` (Node `AgentHandoffItem`) marker (C4).
- V4: `fallback` lowers natively to `FallbackAdapter` (LLM/STT/TTS) — no generated switcher.
- V5: history `full`/`messages`/`last_n`/`summary`/`reset` all compile (all via `ChatContext` construction); `summary` requires a declared+bound `summarizer` profile.
- V6: `human_transfer` — `mode: cold` native; `mode: warm` native (prebuilt `WarmTransferTask`) on **both** SDK languages — stable on Node (no warning), `beta.workflows` on Python (Beta warning); `briefing: summary` supported (consultation flow: TransferAgent summarizes to the operator); `briefing: message`/`wait` → gated error. (review-corrected 2026-07-15: LiveKit's docs badge lags its shipped code; warm is not Python-only.)
- V7: `requires:` guards are generated machine-checked code (code target); a failed guard returns the model a refusal naming the unmet variables (contract).
- V8 (amended 2026-08-08, SCHEMA N19): `greeting` — `speaks_first: user`, fixed `text`, model-written opening all generated; an **absent** block lowers identically to `speaks_first: agent` without `text` (`generate_reply(instructions=...)`), which is now the contractual default shared with the Pipecat driver rather than an incidental one, so the instruction wording is pinned by `TestGreetingDefaultIsModelWrittenOnEveryDriver` and may not move without the Pipecat driver moving with it. A Deepgram driver adopts the same default when it ships. Behaviour unchanged; `on_voicemail` lowers to `AMD` (leave via `generate_reply` then shut down, or hang up); `placement: local` on listen/speak allowed.
- V9: `mcp` tools compile **only with `sdk_language: python`** → gated error on `node`.
- V10: L4 smoke proves the emitted entrypoint imports/validates; safe_core → livekit emits valid, with the expected `TaskGroup`/turn-binding warnings only.
- V11 (amended 2026-07-18 with B6; previously 2026-07-15 with C8): every emitted service class travels with its import and install on one catalogue entry (`TestCatalogInvariants`), an unknown vendor fails with the matrix message and never falls through to a substitute service, and slng entries exist for listen+speak (`TestSlngEverywhere`). The Remy golden asserts the scaffold default still emits `slng.STT`/`slng.TTS` + the native `openai.LLM` (B6); `TestLiveKitV1MultiVendor` asserts per-vendor entries (deepgram/elevenlabs/cartesia) bind with merged `livekit-agents[...]` extras.
- V12: the LiveKit `unmute init` example (`examples/livekit/`) is an invented persona, never the reference Casavo prompt: a greeter that hands off (agent_transfer) to two specialist agents, each running a `TaskGroup` (a journey step, then a shared confirm-contact task). It exercises handoff, task groups, and the SLNG plugin together, and emits valid (V10) with only the permitted `TaskGroup`/turn warnings. The scaffold source package is owned by compiler.md T11; V12 governs only its LiveKit lowering.
- V13 (added 2026-07-16): every `listen`/`speak`/`reason` provider the driver accepts maps to a catalogue entry — import module, `livekit-plugins-<module>` package, api-key env var(s) (or keyless: LiveKit Inference; AWS SDK creds for `aws`), model/voice kwarg names, version floor. No provider silently falls through to a substitute service (the mistake pipecat V11/§B B1 guards). Guarded by `TestLiveKitPluginCatalogCoverage`, mirroring pipecat's `TestServiceInfoCoversEveryMappedClass`; a provider absent from every catalogue and not `slng`/Inference fails loud at generate.
- V14 (added 2026-07-16): validate/generate parity — a spec that validates green for a livekit target must generate (SCHEMA §1 fail-before-generate; PROVIDER_CATALOG F9 one-rulebook). A not-yet-emitted feature gates in the capability table (as pipecat C9 does), never only in a generate-time guard. End state of driver v1: `livekitGuards` and every inline "not emitted yet" refusal are deleted. Guarded by a parity fixture that exercises every SCHEMA §7 livekit-ok feature and must generate clean.
- V15 (added 2026-07-16): the driver consumes every field it validates green — `livekitEmittedFields` agreement test mirrors pipecat's (`pipecatEmittedFields` + its T12 test), so a green-but-ignored field fails a test instead of silently dropping (B1: per-task `model:`). Fail loud is D3; silent drop is the one forbidden outcome.
- V16 (added 2026-07-16): conversation shaping lowers natively — `thinking_audio: subtle` → `BackgroundAudioPlayer(thinking_sound=...)` (`BuiltinAudioClip` preset; verified against livekit-agents source 2026-07-16), `none` → no player; `interruption.minimum_words` → session `InterruptionOptions.min_words` (verified same day: `voice/turn.py`); `ignore_phrases` generated; inactivity/max_duration range-checked per the core warn rows.
- V17 (added 2026-07-18, B4): slng listen/speak routes reach the plugin's `model=` argument **verbatim** — the `slng/` prefix is kept (it names the SLNG-hosted route family and is part of the URL path). No model-form transform strips or rewrites an slng route. Guarded by `TestV17_SlngRouteVerbatim` + the Remy golden.
- V18 (added 2026-07-18, B5): the target's `turn:` binding lowers to the `inference.TurnDetector` version and is never silently dropped — `turn-detector-mini` → `version="v1-mini"` (fully local, no LiveKit Cloud creds), `turn-detector` → `version="v1"` (cloud), binding absent → no `version` kwarg (the SDK auto-selects and falls back to the mini model with a warning, C5); an unrecognized turn model fails loud naming the accepted spellings. Guarded by `TestV18_TurnBindingLowersToDetectorVersion`.
- V19 (added 2026-07-18, B6): a reason vendor with a native catalogue entry binds that plugin and its own key env, never the Inference wildcard; `provider: livekit` is the explicit Inference spelling (model verbatim, `openai/gpt-4o-mini` form). The scaffold default (`provider: openai`) therefore runs `console` with no LiveKit Cloud credentials. Guarded by `TestV19_NativeReasonBeatsInferenceWildcard`.
- V20 (added 2026-07-19, console/web runnable surface; parity [compiler.md](compiler.md)): the emitted `agent.py` ends in `cli.run_app(server)`, so it already ships `console`/`dev`/`start` — this driver emits nothing new for `unmute dev`. Creds matrix (docs.livekit.io/agents/server/startup-modes): `dev`/`start` always need `LIVEKIT_URL`+`LIVEKIT_API_KEY`+`LIVEKIT_API_SECRET`; `console` never connects to a server but still needs `LIVEKIT_API_KEY`+`LIVEKIT_API_SECRET` when any binding lowers to LiveKit Inference (`inference.*`: the `provider: livekit` reason wildcard, or the cloud turn detector `turn-detector`→`v1`) — the scaffold default (native reason V19, local turn mini V18) binds none and runs `console` credless. The CLI reads this off the artifact's `LiveKitInference` list (set in `buildLiveKitData`) rather than re-deriving it; `unmute dev` (web) mints the participant token itself and dispatches the agent by `agent_name` (= target name) via the token's `roomConfig`.
- V21 (added 2026-07-20, B7): tracing verification must run `AgentSession` through a user turn and assert `llm_node` and `llm_request` request spans; a synthetic root span proves exporter connectivity only. Guarded by `TestV22LiveKitSpeechTracingWiring` and the credential-free L4 `TestSmokeV22LiveKitSpeechTracing`; the credentialed probe additionally confirmed Langfuse ingests both request spans as `GENERATION` observations.
- V22 (added 2026-07-20, B8): emitted agents map LiveKit `STTMetrics` and `TTSMetrics` events to completed `stt` and `tts` Langfuse `GENERATION` observations with model/provider and latency attributes. L4 tracing must run one `AgentSession` through the STT and TTS service paths and assert those observations complete under the same session trace as `llm_node` and `llm_request`; text-only `user_input` is insufficient. Guarded by `TestV22LiveKitSpeechTracingWiring` and `TestSmokeV22LiveKitSpeechTracing`.
- V23 (added 2026-07-20, B9): speech tracing is utterance-scoped, not metrics-event-scoped. All `STTMetrics` chunks accumulated before one completed user conversation item become exactly one `stt` generation; all `TTSMetrics` segments accumulated before one completed assistant item become exactly one `tts` generation. Each generation keeps model/provider/latency/usage metadata and sets Langfuse observation input/output (`audio` → transcript for STT, text → `audio` for TTS); the STT transcript and TTS text also update trace input/output. Guarded by `TestV23LiveKitSpeechObservationsAreUtteranceScoped` and the amended `TestSmokeV22LiveKitSpeechTracing`.
- V24 (added 2026-07-20): Langfuse tracing is emitted iff the core IR carries `tracing.provider: langfuse`. Without it, the project contains no Unmute tracing setup, Langfuse/OpenTelemetry-only imports or dependencies, Langfuse environment keys, tracing hooks, or README tracing section. With it, the driver preserves V21–V23's trace/session naming and utterance mappings, lists `LANGFUSE_PUBLIC_KEY`, `LANGFUSE_SECRET_KEY`, and `LANGFUSE_BASE_URL` in `.env.example`, and fails agent startup when any value is missing or empty, including all three. Presence and absence are both locked by L3 goldens; L4 tracing smokes use the configured path.
- V25 (added 2026-07-20, B10): the L4 tracing probe is independent of the selected example's agent and prompt names; changing a configured example's entry-agent identity cannot break the probe before tracing runs. Guarded by `TestSmokeV22LiveKitSpeechTracing`.
- V26 (added 2026-07-20, B11): emitted LiveKit projects declare Ruff and ty as development dependencies and pass `ruff check .` plus `ty check .`; `RunContext`/`function_tool` imports emit only when at least one `@function_tool` method emits, tracing-only types follow V24's configured path and are non-optional, required provider-key lookups are non-optional, and the read-only speech-metrics helper accepts covariant STT/TTS sequences. Guarded by `TestV26LiveKitStaticCheckSurface` and the L4 `TestSmokeV26LiveKitExamplesStaticCheck`, which checks every public example plus configured and unconfigured tool-free variants.
- V27 (added 2026-07-20, SCHEMA N18): the generated README carries a Deploy section with the exact `lk agent create` command; the command includes `--region <value>` exactly when the target instance declares `deployment_region` and omits the flag otherwise. `livekit.toml` never carries a region — doc-verified 2026-07-20 (docs.livekit.io agent-deployment regions): region is a create-time CLI flag (`us-east|eu-central|ap-south` today), immutable after creation, no toml key exists. The value is forwarded as declared, never validated; the `lk` CLI is the real validator.

## §T tasks
id|status|desc|cites
T1|x|LiveKit capability rows in the core table: TaskGroup→warn(Python only), warm→native both langs (Beta-warn Python only), briefing summary-only, mcp→python-only, turn binding→warn; else code-target defaults|C3,C5,V6,V9
T2|x|`GenerateLiveKit` skeleton wired into the `generate.Artifact` switch|I.emit
T3|x|`templates/livekit_v1/*.tmpl` — entrypoint, agents (handoff), pipeline roles (SLNG recipe), webhook tools, greeting; telephony (outbound dial-out + AMD, T6) and deploy files (Dockerfile, livekit.toml) emitted|I.templates,C2
T4|x|`AgentTask` class + group-step path (typed result via `finish`) + task_group shared → TaskGroup(`summarize_chat_ctx=False`)+N13 snapshot/restore; single-task delegates landed at T12, isolated sequences at T13|V1,V2,V3,C3,C4
T5|x|native `FallbackAdapter` fallback; `ChatContext` history for all five values + summarizer|V4,V5
T6|x|human_transfer cold (`JobContext.transfer_sip_participant` REFER) / warm (`WarmTransferTask`, Beta-on-Python note, `LIVEKIT_SIP_OUTBOUND_TRUNK`); `requires` guards refuse naming unmet userdata vars; outbound dial-out (`CreateSIPParticipantRequest` off job metadata) + `AMD` voicemail with leave_message/hangup branches|V6,V7,V8
T7|x|framework version range check (>=1.5,<2.0); `sdk_language` other than python fails loud (python templates only — subsumes the mcp/node gate, V9); plugin `pins:` raise dep floors, unknown keys and below-catalogue-floor values fail loud|C1,C6,C7,V9
T8|x|golden test (Remy, byte-for-byte) + `py_compile`; L4 smoke wired (`livekit_v1_smoke_test.go`, `-tags smoke`): uv resolves the emitted pyproject, agent.py imports, every generated Agent/AgentTask instantiates against real livekit-agents + plugins — Remy (slng) and safe_core multivendor (deepgram/elevenlabs/cartesia + cold human_transfer) both pass|V10
T9|x|entrypoint/session template wires the SLNG recipe: `slng.STT`/`slng.TTS` for SLNG-routed listen/speak (route as `model=`), `inference.LLM` for reason, `inference.TurnDetector`, `silero.VAD`, optional `ai_coustics` NC; golden asserts the `slng` import + construction; native per-vendor STT/TTS/LLM plugins emitted alongside via the provider catalogue with dynamic imports/deps/env (amended 2026-07-16)|C8,V11,V13
T10|~|`internal/testdata/remy/` source package compiles to a valid LiveKit project (handoff + task groups + SLNG); golden is the tracked output; a checked-in `examples/livekit/` dir + compiler.md T11 init scaffold still pending. invented "Remy" restaurant concierge; a greeter with agent_transfer to a reservations agent and an events agent, each a `TaskGroup` (qualify, then a shared confirm-contact task); SLNG-routed listen/speak; source package via compiler.md T11|V12,V2,V3
T11|x|provider catalogue pilot: listen/speak/reason resolve via `internal/target` catalogue entries (slng default + deepgram listen, elevenlabs/cartesia speak, Inference wildcard reason); plugin imports merged into one line; extras merged onto the livekit-agents pin; matrix-quoting no-slot errors|C8,V11
T12|x|single-task delegate: `@function_tool` awaits the `AgentTask` (empty ctx, typed-result-only return, C4); `assign` maps `result.<field>` → typed userdata vars (N5); lift the "task-group delegates only" refusal in `buildLiveKitDelegate`; variables lower to a `Userdata` dataclass on `AgentSession[Userdata]`|V1,V3,V14
T13|x|isolated task_group → generated standalone-`AgentTask` sequence, no TaskGroup (C3); lift the isolated guard|V2,V14
T14|x|per-task `model:` → `AgentTask` with its own `llm=` resolved through the catalogue (native per-agent/task override, same path as `buildLiveKitAgent`'s `def.Model != entry.Model`); the strict `livekitEmittedFields` agreement test lands at T16 when every field is settled (B1)|V1,V15
T15|x|agent-level webhook tools on `Agent`s (shared `webhook_tool` define with tasks, `effect: ends_conversation` → shutdown); conversation shaping: `thinking_audio` → `BackgroundAudioPlayer(thinking_sound=)`, `interruption.enabled`/`minimum_words` → `TurnHandlingOptions.interruption`, `ignore_phrases` → generated `stt_node` filter mixin, inactivity → `user_away_timeout` + away handler, `max_duration` → asyncio timer|V14,V16
T29|x|`execution: builtin` prebuilt tools (added 2026-07-22): `end_call` lowers to the beta `from livekit.agents.beta.tools import EndCallTool`, constructed with `extra_description`/`end_instructions` and spread into `super().__init__(tools=[...])` on agents and tasks (not a `@function_tool` method); `FieldToolBuiltin` core, `tools.execution.builtin` emitted. Full design in [prebuilt-tools.md](prebuilt-tools.md)|prebuilt-tools
T16|x|parity closeout: `TestLiveKitV1ParityFixture` (every §7 livekit-ok feature at once) generates clean; `livekitGuards` + inline refusals deleted; `livekitEmittedFields` + `TestLiveKitEmitterMatchesCapabilityTable` (V15) land; SCHEMA §7 livekit column verified accurate as-is, no §9 livekit row needed. Two reconciling table rows added: `FieldSpeakEndpoint` deny(livekit) (openai.TTS has no language slot, N14 — matches CheckVendor's existing refusal) and `FieldToolInterruption` warn(livekit) (tools run to completion; SCHEMA §5 note amended 2026-07-16)|V14,V15,B2
T17|x|restore the PR #10 provider breadth (silently reverted by the catalogue reconcile b4c92c8, [compiler.md](compiler.md) B4) as catalogue entries in `catalog_livekit.go`: listen `cartesia`, `elevenlabs`, `gradium`, `sarvam`, `soniox`, `assemblyai`, `speechmatics`; speak `deepgram`, `inworld`, `rime`, `gemini`, `gradium`, `sarvam`, `soniox`; reason native per-vendor LLM plugin entries where PR #10 had them (`anthropic`, `aws`, `groq`, `mistralai`, `openrouter`, `sarvam`, `azure`), keeping the Inference wildcard as the default. Recover module / package / key envs / model-voice kwargs / version floors from `git show 489570a` (the recovered quirks are per-entry facts, F3/F4: `ELEVEN_API_KEY`; gradium `model_name`+`voice_id`; rime + sarvam `speaker`; gemini `google.beta.GeminiTTS` `voice_name`; speechmatics no model kwarg; soniox `STTOptions`; aws multi-env SDK creds; azure/openrouter classmethod constructors), but **re-verify every claim against current docs.livekit.io (context7 or WebFetch) — never from the diff or memory alone**; each entry carries `Verified` + `Docs` ([compiler.md](compiler.md) V21) per the PROVIDER_CATALOG.md §5.1 recipe. Extend `CallSpec` only where a verified constructor shape demands it (classmethod constructors, multi-env creds) — no speculative fields. SLNG stays the scaffold default; additive; the Remy golden stays byte-identical. Refresh `TestCatalogResolutionGolden` (`-update-catalog`) + livekit goldens; update `docs/user/reference/providers.md` + the sync test ([compiler.md](compiler.md) V20)|C8,V11,V13,compiler.md B4/V20/V21
T18|x|listen (STT) fallback lowering (compiler T16, 2026-07-19): the selected listen model's chain renders as native `stt.FallbackAdapter(stt=[...])` (adapter verified in the livekit-agents source checkout 2026-07-19: `stt/fallback_adapter.py`, ctor `stt: list[STT]`); each chain entry resolves through the same catalogue path as the primary (per-vendor plugin, key env, extras dep); the `stt` module import emits with a chain; `livekitEmittedFields` gains `FieldListenFallback` (V15 agreement holds); `TestT16_LiveKitEmitsListenFallbackAdapter`|C8,V11,compiler.md T16
T19|x|Bridge LiveKit speech metrics into Langfuse generation observations; add a deterministic smoke that exercises STT audio, LLM, and TTS synthesis in one `AgentSession`; credentialed console + Langfuse API probe confirmed `stt`/`tts` `GENERATION` observations with model/timing data on one trace|V22,B8
T20|x|Aggregate speech usage metrics at completed conversation-item boundaries and populate Langfuse input/output fields without changing any provider constructor or endpoint|V23,B9
T21|x|Make all existing Langfuse setup, speech mapping, imports, dependencies, environment keys, hooks, and generated README text conditional on core tracing config; replace the all-empty runtime disable path with a required-credentials error; add configured/unconfigured goldens and keep the request+speech L4 smokes on the configured fixture|V24,V21,V22,V23,compiler.md T17
T22|x|make generated LiveKit Python Ruff/ty-clean; declare the checkers in the emitted project and check every public example plus configured and unconfigured tool-free fixtures|I.templates,C1,V10,V19,V23,V24,V26,B11
T23|x|`deployment_region` lowering (SCHEMA N18, compiler T22): `README.md.tmpl` gains a Deploy section with the `lk agent create` command, `--region <value>` iff the instance sets `deployment_region`; `livekit.toml.tmpl` untouched (no region key exists, V27); goldens for the set and unset shapes|V27,compiler.md V35

Dependency order: T1, T2 → T3 → T4, T5, T6 → T7 → T8. T9 with T3 (entrypoint template); T10 after T4 + T9 and after compiler.md T11 scaffolds the source package. T11 after T9. T12, T13, T14 after T4; T15 with T3 (template work); T16 last, after T5, T6, T12–T15. T17 after T11 (extends the catalogue + its goldens); T19 after T8; T20 after T19. T21 after compiler.md T17 and T20; T22 after T21.

## §B bugs
id|date|cause|fix
B1|2026-07-16|per-task `model:` validated green on livekit (`FieldTaskModel` denies pipecat only, `internal/target/table.go`) but `buildLiveKitTask` never read `task.Model` — the override was **silently dropped**, the task ran on the session LLM. No livekit emitted-fields agreement test existed (pipecat has `pipecatEmittedFields`), so a green-but-ignored field had nothing to fail. Found 2026-07-16 doc/code alignment audit.|Interim: per-task model added to `livekitGuards` (fail loud, D3) until T14 emits the lowering. Structural: new V15 (`livekitEmittedFields` agreement test) so the whole class — field validates green, driver ignores it — fails a test. Fix lands with T14.
B2|2026-07-16|the driver refused `fallback`, history ≠ `full`, `requires`, `thinking_audio`, `human_transfer`, `outbound`/`on_voicemail`, isolated groups, single-task delegates, and agent-level tools at **generate** time while the capability table validated them all green — validate-green/generate-fail, violating SCHEMA §1 (clear error before anything is generated) and PROVIDER_CATALOG F9 (one rulebook). Root cause: maturity gates lived only in `livekitGuards`/inline errors; no table rows, no SCHEMA §9 livekit row, and the driver specs described pipecat and livekit as equally mature. Found 2026-07-16 doc/code alignment audit.|Mandate is implement, not gate: T5, T6, T12–T15 emit the missing lowerings; new V14 (validate/generate parity fixture) makes the class unrepresentable; T16 deletes the guards as each lowering lands. Generate-time refusals stay until then — loud-but-late beats silent.
B3|2026-07-16|SCHEMA §5 promised `mcp` tools on LiveKit (Python), but the schema had **no slot for the MCP server address**: `url_env` was legal for `webhook` only, and no other tool or target field could carry an endpoint. Any code-target mcp lowering was structurally impossible; the §7 "ok" cell was unimplementable as written. Found implementing T16.|SCHEMA §5 amended (2026-07-16): `url_env` is legal for `webhook` and `mcp`; for `mcp` it names the server address; managed targets may configure the server provider-side and ignore it. `ir.Validate` requires it for mcp tools. LiveKit lowers each agent's mcp tools to `mcp.MCPServerHTTP(url=os.environ[url_env], allowed_tools=[...])` on `mcp_servers` (allowed_tools keeps D8: visibility decided by the tools: list).
B4|2026-07-18|`FormSlngRoute` stripped the `slng/` prefix (C8 as written 2026-07-15), but the SLNG API's hosted route family keeps that prefix in the URL path — the plugin interpolates `model=` verbatim into `/v1/{stt,tts}/{route}`, so every scaffold-default binding 404'd at runtime (`/v1/stt/deepgram/nova:3-en` → 404; `/v1/stt/slng/deepgram/nova:3-en` → 401 auth-required, route exists). Root cause: C8 conflated the two route families — its examples (`deepgram/nova:3`) were proxied-family spellings; the pipecat slng entry never stripped and was fine. Found running mytestagent `console` 2026-07-18.|C8 amended: routes pass verbatim; `FormSlngRoute` deleted (default is already verbatim); new V17 + `TestV17_SlngRouteVerbatim`; Remy golden + docs/user prose refreshed.
B5|2026-07-18|`TurnVersion` was hardcoded `"v1"` in `buildLiveKitData` — the target's `turn:` binding (the scaffold writes `turn-detector-mini`) was **silently dropped** (B1's green-but-ignored class, but on a targets.yaml field, outside `livekitEmittedFields`' agent.yaml scope), and an explicit `version="v1"` turns missing LiveKit creds into a constructor `ValueError` instead of C5's automatic mini fallback → `console` demanded cloud creds nobody asked for. Found running mytestagent `console` 2026-07-18.|new V18: the turn binding lowers to the detector version (`turn-detector-mini` → local `v1-mini`; absent → auto, C5); unrecognized model fails loud. `TestV18_TurnBindingLowersToDetectorVersion`.
B6|2026-07-18|reason `provider: openai` had no native catalogue entry (T17 restored seven vendors but not openai — "stays on the wildcard" was deliberate but wrong for local runs), so the scaffold default fell to the Inference wildcard: emitted `inference.LLM("openai/gpt-4.1-mini")` requires `LIVEKIT_API_KEY`/`LIVEKIT_API_SECRET` even in `console` mode and never reads `OPENAI_API_KEY` — the user's explicit vendor choice was silently rerouted through LiveKit Cloud billing and local runs were impossible without cloud secrets. Found running mytestagent `console` 2026-07-18.|native `openai` reason entry (doc-verified `openai.LLM` / `OPENAI_API_KEY` / `livekit-agents[openai]`, docs.livekit.io + plugin source 2026-07-18); `provider: livekit` special-cased in the wildcard model form as the deliberate Inference spelling; C8/V11 amended; new V19 + `TestV19_NativeReasonBeatsInferenceWildcard`.
B7|2026-07-20|The Langfuse verification created a manual root span and asserted export, so it certified credentials and the shared tracer provider without running `AgentSession` or an LLM request. Langfuse correctly showed only `SPAN` rows; no model request had run.|V21: add a deterministic request-level L4 smoke and document that one user turn is required. A live `AgentSession.run` request was queried through the Langfuse API and returned `GENERATION` observations for `llm_node` and `llm_request`.
B8|2026-07-20|The tracing smoke injected text into `AgentSession(llm=FakeLLM())`, so no STT or TTS path ran. The supported LiveKit runtime emits speech metrics, but those events were only logged; no stable Langfuse `stt`/`tts` generation observations carried their latency data.|V22 + T19: map speech metrics to completed generation spans and exercise both speech services in L4.
B9|2026-07-20|The V22 handler created a generation for every `metrics_collected` event. Streaming STT emits usage events repeatedly across one utterance, so one call flooded the trace with zero-duration `stt` rows; metrics objects also contain no transcript or synthesized text, leaving Langfuse input/output blank. TTS segment metrics had the same missing semantic payload.|V23 + T20: buffer STT/TTS metrics and flush once when LiveKit adds the completed user/assistant conversation item; aggregate timing/usage metadata and attach Langfuse observation + trace input/output fields.
B10|2026-07-20|The configured tracing smoke selected `examples/simple-prompt` but referenced `WELCOME_AGENT_PROMPT`, an identifier from an older fixture, so the smoke failed before tracing ran.|V25: make the probe prompt independent of generated fixture names.
B11|2026-07-20|No generated-project static-check gate existed: the template imported `RunContext`/`function_tool` unconditionally, annotated two concrete metric lists as one invariant mutable list, and passed optional `os.environ.get` results to required provider-key parameters. PR #27 also made configured tracing mandatory while retaining an optional tracer-provider return type. The tool-heavy public examples hid the Ruff failure.|V26 + T22: conditional function-tool imports, tracing-conditional covariant and non-optional types, non-optional environment indexing, Ruff/ty development dependencies, and an L4 check over every public example plus configured and unconfigured tool-free variants.
